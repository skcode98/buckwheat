---
description: Build the debug APK
agent: build
---

Build the app and report the result.

1. Run `.\gradlew.bat assembleDebug`.
2. If the build fails, fix the error at its source, then rebuild.
3. On success, report the APK path: `app\build\outputs\apk\debug\`.
4. Keep the build green before finishing.
