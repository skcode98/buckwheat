# Memory & Decisions

## Active Context
- **Base branch**: `master` — clean fork of `danilkinkin/buckwheat` upstream/master @ `4b60102`
- **Our saved work**: `our-fixes` branch on origin (all prior changes preserved there)
- **Backup branch (2026-08-09)**: `backup/pre-trackinvest-2026-08-09` on origin — full snapshot of `master` @ `bc5e922` (everything achieved so far, see "Achieved so far" below), created BEFORE starting the TrackInvest web-app → Android migration. Master keeps moving past this snapshot; the backup is the frozen pre-migration state.
- **Upstream**: `https://github.com/danilkinkin/buckwheat.git`
- **Origin**: `https://github.com/skcode98/buckwheat`

### Achieved so far (as of backup `bc5e922`, 2026-08-09)
Voice widget suite (5 designs: Percent left, Amount left, Ring, Graph background, + render fixes), voice AI parsing with live free-model dropdown + reliable default (`openai/gpt-oss-20b:free`, centralized defaults), widget failure surfacing (`AI off: <reason>`), midnight widget refresh (`setWindow`), AI spend categories + emoji picker + category management, full JSON backup & restore, daily budget reminder + on-track overspend alert + instant overspend notification (all `setWindow`-based now except `OnTrackAlertScheduler`), analytics suite (trend card, weekday breakdown, vs-previous, calendar heatmap, share summary), recurring payments, savings goals, CSV import/export, currency (INR default), tags, custom keyboard + batch voice input, **plus 2026-08-10: round values toggle, end-of-period spend forecast, recurring-payment due alerts, savings-goal milestone nudges, weekly/monthly spend digest, category drill-down analytics** (`89840b5`, `b898654`, `31e02b7`, `8f04f0e`, `1314350`, `372b3c5`). **183 unit tests green**, golden pipeline (`spotlessApply` + `testDebugUnitTest` + `assembleDebug`) green. Feature history in `CHANGELOG.md`.

## Current State
- **2026-08-12 Category budget utilization → battery indicators in Analytics (SHIPPED — committed `b09cd56`, pushed; golden pipeline green — 290 tests, 0 failures, APK built)**: user request — the category-caps UI in `SpendCategoriesCard` should read like a battery: used budget = filled portion, remaining = unfilled, shown directly on the category pill, with category name + amount used + used percentage. New `analytics/categoriesChart/CategoryBatteryChip.kt` (full-width pill: track = category color @15%, fill over left `categoryBatteryFraction` of the pill, amber ≥80%, error red ≥100%, 3dp nub on the right; label overlays the pill via a clipped two-layer draw — `drawWithContent` + `clipRect(right = fillW)` re-renders the text row in the fill-contrast color inside the fill region). Capped categories use the battery chip (tap still opens `CATEGORY_HISTORY_SHEET`); uncapped keep `TagAmount`. New pure `categoryBatteryFraction` in `CategoryCap.kt` (must use `divide(cap, 4, HALF_UP)` — Kotlin `BigDecimal.div` rounds to scale 0, so 50/100 → 0; caught by the new test). New EN string `category_battery_percent` `%1$d%%`. `CapProgressBar` kept (InterleavedBudgetCard still uses it). 1 new `CategoryCapsTest` case (8 asserts).
- **2026-08-12 Tap-to-edit + long-press in History (backlog #1) — shipped (committed `e98dccc`, pushed)**: new `history/SpentItemActions.kt` wraps `SpentItem` with `combinedClickable` — tap = edit (`startEditingSpent` + `onClose()`, same as swipe-to-edit), long-press = `DropdownMenu` (Edit / Delete / Copy). Delete = `removeSpent` (instant, matches swipe). Copy = `LocalClipboardManager` with formatted amount + comment via pure `buildSpendCopyText` (separator ` — `) + snackbar `history_actions_copied`. Wired only in the non-readOnly branch (read-only `ViewerHistory`/`SearchHistorySheet` stay plain). New `ic_content_copy.xml`, 4 EN strings (`history_actions_*`), 3 new `SpentItemActionsTest` cases (286 → 289).
- **2026-08-12 UI cleanup (committed `10c3693`, pushed): AI report → Settings + simpler interleaved/caps UI**. User feedback: AI report is "just normal text" in the wrong place, interleaved + category-caps UIs are messy. Changes: `analytics/AiInsightCard.kt` deleted, `Analytics.kt` cleaned (no more `AiInsightViewModel`), new `settings/AiInsightSheet.kt` (`AI_INSIGHT_SHEET`, registered in BottomSheets) + Settings row under "Voice AI"; report bullets now render as bullet rows (`ReportBody`) instead of a text wall. `InterleavedBudgetCard` rows → right-aligned "₹X / ₹Y" + progress bar + single caption `"12 Aug – 10 Sep · runs out on 12 Sep"`; velocity sentence removed (strings shortened, new `interleaved_window_status`). `CategoryCapsSheet` → 4 equal-width FilterChips replace the ExposedDropdownMenuBox per row; anchor = clickable "Starts 12 Aug 2026" caption (new `category_caps_anchor`); dropped "Quick templates" label. **286 tests, 0 failures**, golden pipeline green.
- **2026-08-12 Docs overhaul + first backlog feature shipped (committed `10c3693`, pushed)**: all `.track` md files reviewed/updated; **new `DECISIONS.md`** (every decision + why, newest-first) and **new `FEATURES_AND_IMPROVEMENTS.md`** (20 candidates verified missing by a codebase-wide exploration, Tier 1/2/3, each with files/complexity/value/status). From that backlog picked **Repeat-last-spend editor quick action** (#3, Tier 1: Small complexity, High daily value) and implemented it: pure `editor/RepeatLastSpend.kt` (`lastSpendToRepeat` filters SPENT, most recent by date then uid), `EditorViewModel.startRepeatSpend` (prefills value/comment/category, date=today, `editedTransaction=null`, `mode=ADD`, `stage=EDIT_SPENT` → confirming creates a NEW row through the untouched commit path), `AssistChip` in `Editor.kt` (shown only in ADD mode, fed by `periodSpends`, `ic_autorenew` + amount + comment, `.trim()` on empty comment), string `repeat_last_spend`. 5 new `RepeatLastSpendTest` cases. **286 tests, 0 failures**, golden pipeline green. (Same commit also carries the UI-cleanup work above — strings.xml was shared between the two.)
- **2026-08-11 Big batch (5 items) shipped (committed A `b23b12d`, B `c39e21b`, C `124491e`, D `d61edc4`, E `84f5aaf`, all pushed)**: A) On-track overspend alert — last `setRepeating` alarm → `setWindow` (10-min window) in `OnTrackAlertScheduler`; `OnTrackAlertReceiver` re-arms next day ONLY while the toggle is still enabled; `ReminderBootReceiver` now reschedules it on boot (was missing). B) Archived-transaction categories — Room **v13 → v14** (`AutoMigration13to14` ALTER `archived_transactions` ADD `category`); `ArchivedTransaction.category` + `toTransaction()` carries it; `archiveImported` persists it; BackupData codec round-trips it; new `BudgetPeriodDao.updateCategory`; `SpendCategoriesViewModel.categorizeUncategorized()` split into `categorizeTransactions()` + `categorizeArchived()` so the WHOLE history (active + archived) gets offline-first-then-AI persisted categories. C) Interleaved **Phase 5** shipped (mitigated overlay): pure `dailyPace(category)` = DAILY→amount else amount/(freqMonths×30) (per-day share — the plan's own "≈₹166/day" example; the draft's literal `monthlyEquivalent` would over-reserve ~30×), `applyCategoryAllowance(raw, allowance) = max(raw−allowance, raw×0.8, 0)` scale-2, applied only at `whatBudgetForDay`/`nextDayBudget` returns; `updateDailyBudget`/`setDailyBudget` untouched; no schedules → byte-identical. D) Interleaved **Phase 6 engine** built + tested (`interleaved/AnalyzeSpendingPatterns.kt`: `suggestFrequency` median-gap rules MONTHLY 20–45d / QUARTERLY 60–120d / ANNUAL span≥330d, `medianAmount` HALF_EVEN, `analyzeSpendingPatterns`; `SettingsRepository.applyScheduleSuggestions` seeds schedule+cap only for categories with NO existing schedule — user choices win). Real calibration still blocked on the user's 6-month export. E) Past-period analytics: `settings/PeriodDetailSheet` now renders SpendCategoriesCard / CategoriesChartCard / SpendsTrendCard / SpendsWeekdayCard for every period with spends (incl. imported month-bucket periods). Entry point already existed (**Settings → Past periods**), so no duplicate Analytics button was added. Golden pipeline green — **281 tests, 0 failures** (+23: InterleavedBudgetTest +4, InterleavedAllowanceTest 5, AnalyzeSpendingPatternsTest 10, ImportAutoCategorizeTest +2, InterleavedRolloverTest +2; BackupDataTest fixture now carries category). Gotchas recorded: `countDays` is inclusive (`plusDays(9)` → restDays 10); `BigDecimal.max`/subtraction are scale-sensitive — `applyCategoryAllowance` normalizes with `setScale(2)`; `FakeBudgetPeriodDao.updateCategory` must re-apply uid after `copy` (same as `updateTotalSpent`); two txs per month in a miner test create zero-gaps that break cadence detection. Plan: `.track/BIG_BATCH_PLAN.md` (Task C formula corrected in-doc).
- **2026-08-11 Auto-categorization persisted to DB shipped (committed `550679d`, pushed)**: user asked for "AUTOCATEGORIZATION for the imported data and save it in db, no need to load again from AI". Editor `CategorySelector` + analytics `SpendCategoriesCard` already exist, so this made categorization persistent & complete: new pure `offlineCategoryOrNull()` in `SpendCategorizer.kt` (keyword match to persist, null for OTHER); `SpendsRepository.importTransactions` writes offline categories onto imported rows before insert + runs the AI batch pass (`persistAiCategories`) over still-uncategorized imported rows and persists via `updateCategory(uid, …)` (no-op without API key / toggle off); `SpendCategoriesViewModel.categorizeUncategorized()` now scans ALL SPENT rows in the transactions table, persists offline keywords first then AI for the rest — Analytics triggers it on open but results are persisted so no AI reload is ever needed. Tests: `ImportAutoCategorizeTest` (3, Robolectric) + 2 `offlineCategoryOrNull` cases. Golden pipeline green (258 tests, 0 failures). Note: `setBudget` inserts `SET_DAILY_BUDGET` transaction rows, so import tests must filter `SPENT`.
- **2026-08-11 AI Intelligence report shipped (committed `b45a76c`, pushed)**: analytics card at the top of the analytics column (above `WholeBudgetCard`). New `ai/AiInsight.kt` (pure: `SpendInsightSummary`, prompt builders, `parseAiInsightReport` cleanup, `overspendDayCount`, `generateAiInsight` HTTP following VoiceAi conventions — 10s/20s timeouts, `disconnect()` in `finally`, `extractModelContent`, 200-char truncation, `NotConfigured` when key missing or `aiIntelligenceEnabled` off) + `ai/AiInsightViewModel.kt` (`state` LiveData, `generate()` duplicate-guarded, `buildSummary()` from SpendsRepository flows, `findPreviousPeriod`, `categoryTotals`) + `analytics/AiInsightCard.kt` (Idle/Loading/Report/Error/NotConfigured bodies, `ic_analytics`, "Set up AI" → `PathState(VOICE_AI_SETTINGS_SHEET)`). 9 new EN strings. 10 new tests. Golden pipeline green (253 tests, 0 failures). First-compile lessons: `@HiltViewModel` needs the explicit `dagger.hilt.android.lifecycle.HiltViewModel` import; `getFinishPeriodActualDate()`/`getFinishPeriodDate()` are `Flow<Date?>` so the finish-date chain needs `?: getCurrentDateUseCase()`. Plan doc: `.track/AI_INTELLIGENCE_REPORT.md`. Interleaved Phases 5/6 remain deferred (wallet integration / 6-month calibration).
- **2026-08-11 Interleaved budgets Phase 4 shipped (committed `8c8eabf`, pushed)**: Analytics card. New `analytics/InterleavedBudgetCard.kt` renders after `WholeBudgetCard` in `Analytics.kt` (the plan's "after SpendsBudgetCard" was wrong — `SpendsBudgetCard` is only ever previewed, never composed in Analytics), self-hides when no schedules exist. Per category: emoji + localized name, window range ("1 Sep – 30 Sep" = `windowStart`…`windowEnd−1`, engine windows are half-open), "₹1,200 of ₹5,000" amounts, reused `CapProgressBar` (made `internal`), velocity line ("at ₹X/day → runs out on <date>" via `projectedExhaustionDate`, "on track for this window", or "Nothing spent yet"). `SpendsRepository.getInterleavedProgress(): Flow<List<InterleavedProgress>>` = `combine`(schedules+caps DataStore flow, `transactionDao.getAll().asFlow()`) → per-category `windowFor`/`windowSpent`, skips DAILY + zero-amount, sorted by name; observed via `spendsViewModel.interleavedProgress.asLiveData()`. 6 new strings. Golden pipeline green (243 tests, 0 failures).
- **2026-08-11 Interleaved budgets Phase 3 shipped (committed `8912b89`, pushed)**: Settings UI. `CategoryCapsSheet` rows gained an `ExposedDropdownMenuBox` frequency picker (DAILY/MONTHLY/QUARTERLY/ANNUAL; DAILY = plain cap → removes schedule) + tappable anchor date (`prettyDate`, opens `InterleavedAnchorSheet` single-day `DatePicker`, writes via `setAnchor` preserving frequency). Three quick-template `AssistChip`s (Monthly Essentials FOOD/TRANSPORT/BILLS @5000, Quarterly Big Tickets HEALTH/SHOPPING/ENTERTAINMENT @15000, Annual Obligations TRAVEL/BILLS @60000 — built-in stored names so window math matches persisted category values; default amounts seed caps to avoid zero-amount schedules that can never cross). `CategoryCapsViewModel` exposes `interleaved` LiveData (caps+schedules `combine`) + `setInterleaved`/`setAnchor`/`applyTemplate` (all single `edit {}` via `setCategoryCapsAndSchedules`). 13 new strings. `InterleavedAnchorSheet` registered in BottomSheets as `INTERLEAVED_ANCHOR_SHEET`, wired from `CategoryCapsSheet` via `onEditAnchor` → `appViewModel.openSheet`. Golden pipeline green (243 tests, 0 failures) after fixing a pre-existing flaky `NumberFormatTest` race (Application DataStore collector clobbers direct `roundValues` writes; test now drives the flag through the DataStore + waits).
- **2026-08-11 Interleaved budgets Phase 2 shipped (committed `ec77e9b`, pushed)**: `categorySchedulesStoreKey` codec (`"name:frequency:anchorEpochDay;..."`, defensive parse) + window-aware notified codec (`"name:bucket@windowStartEpochDay"`, legacy `"name:bucket"` → sentinel `Long.MIN_VALUE`) in `SettingsRepository.kt`; `getCategorySchedules`/`getInterleavedCategories` flows + `setCategoryCapsAndSchedules` (single-edit). `SpendsRepository`: cap alerts window-aware (`categoryProgressTotal` = windowSpent for scheduled, period total for plain caps), `resyncInterleavedNotified()` resets rolled buckets on addSpent/removeSpent, `clearCategoryCapNotifiedNow` keeps windowed entries on period change (DAILY = plain cap). Tests: `InterleavedScheduleCodecTest` (11) + `InterleavedRolloverTest` (4) green; 243 total. Commit message body was trimmed by PowerShell `...` splatting (header intact, not force-pushed).
- **2026-08-11 Interleaved budgets Phase 2 shipped (committed `ec77e9b`, pushed)**: `categorySchedulesStoreKey` codec (`"name:frequency:anchorEpochDay;..."`, defensive parse) + window-aware notified codec (`"name:bucket@windowStartEpochDay"`, legacy `"name:bucket"` → sentinel `Long.MIN_VALUE`) in `SettingsRepository.kt`; `getCategorySchedules`/`getInterleavedCategories` flows + `setCategoryCapsAndSchedules` (single-edit). `SpendsRepository`: cap alerts window-aware (`categoryProgressTotal` = windowSpent for scheduled, period total for plain caps), `resyncInterleavedNotified()` resets rolled buckets on addSpent/removeSpent, `clearCategoryCapNotifiedNow` keeps windowed entries on period change (DAILY = plain cap). Tests: `InterleavedScheduleCodecTest` (11) + `InterleavedRolloverTest` (4) green; 243 total. Commit message body was trimmed by PowerShell `...` splatting (header intact, not force-pushed).
- **2026-08-11 Interleaved budget engine: Phase 1 shipped (committed `bce0be8`, pushed)**: pure `interleaved/InterleavedBudget.kt` (no Android/DataStore/Room imports) + `InterleavedBudgetTest` (28 tests). `CategoryFrequency` (DAILY/MONTHLY/QUARTERLY/ANNUAL), `InterleavedCategory` (name, amount, frequency, anchorEpochDay), `WindowSpend` (date, value, category). `windowFor` = calendar-month `[start, end)` via `java.time` (clamps month-end + leap years; DAILY → null; pre-anchor → first window). `hasRolled` compares recorded window start (sentinel `Long.MIN_VALUE` forces reset; DAILY never rolls). `windowSpent` sums in-window matching txs (half-open end, future excluded, case-insensitive name). `monthlyEquivalent` = amount/freqMonths (DAILY → amount). `daysLeftInWindow` incl. today, restarts each window. `projectedExhaustionDate` = dry-out day at current pace (null when nothing spent or window ends first). Open questions auto-resolved (plan defaults): calendar months, no untracked, Phase 5 stretch only. Phase 3 next: Settings UI (`CategoryCapsSheet` frequency dropdown + anchor picker + templates).
- **2026-08-11 Settings reorganization + analytics calendar fixes + AI master toggle (committed `e87bded`, docs `b5af869`, pushed)**: (1) `SpendsCalendar` heatmap fixed — days showing red when within budget because same-day `SET_DAILY_BUDGET` rows were being *added to the day's spend* (order-dependent builder) and the fallback budget was 0; now order-independent (spends = SPENT only, budget = last per-day SET_DAILY_BUDGET override else current `dailyBudget` passed from Analytics), green-blend division guarded. Month header was bottom-aligned (`Alignment.Bottom`) while the arrows are centered — now `CenterVertically`. (2) Notification settings moved from `Settings.kt` into a new `NotificationsSheet` submenu (`NOTIFICATIONS_SHEET`), 5 rows, registered in `BottomSheets`. (3) "AI Intelligence" master toggle added at top of the Voice AI submenu: new `aiIntelligenceEnabledStoreKey` (default on), pure `aiIntelligenceEnabled(prefs)` gate used by both `parseVoiceInputWithAi` (→ NotConfigured, offline parser) and `categorizeSpendsWithAi` (→ empty, offline classifier). 4 new gate tests. **201 tests green**, full pipeline green.
- **2026-08-11 Interleaved-budget plan revised to v2 (committed in docs `b5af869`, pushed)**: `.track/INTERLEAVED_BUDGETS.md` rewritten. v1 proposed a parallel Room `BudgetCategory` entity + DB migration 14→15; v2 reconciles the feature with the shipped Feature 9 caps — interleaved budgets are now an *extension* of caps (new DataStore key `categorySchedulesStoreKey` = `"name:frequency:anchorEpochDay;..."`, **no migration**; current DB version is 13, untouched). Corrected v1 factual errors: kotlinx-datetime is NOT in deps (use `java.time`, codebase standard); `changeDayAction()` doesn't exist; caps are DataStore-based, not Room. Window math uses calendar months (`ChronoUnit.MONTHS.between` + `plusMonths(freqMonths)`), handled in pure `interleaved/InterleavedBudget.kt`. 5 phases + 2 stretch/deferred.
- **2026-08-10 Bug fix: vs previous analytics disappears on early period restart (committed `52fc39c`)**: when a new budget period started before the old period's scheduled finish date, the "vs previous" comparison card vanished after 1-2 recompositions. Root cause: `archiveCurrentPeriod()` saved the archived period with the old *scheduled* `finishDate` instead of capping it at the new period's start date, so `findPreviousPeriod()` filtered out the just-archived period. Fix: `archiveCurrentPeriod()` now caps `finishDate` (and `actualFinishDate`) at the new period's start date; `inPeriod` transaction filter uses the capped date. Verified: 200 tests (197 + 1 new regression test), `assembleDebug` green.
- **2026-08-10 Feature 9: Category spending caps (committed `7f9d481`, pushed)**: each spendable category can now have a per-category monetary cap. When cumulative period spend reaches 80% of the cap, a near-threshold notification fires; at 100%, a reached notification. Progress shown as a progress bar under each Analytics category chip. Caps reset on new budget period. All code written (13 files), tests (10 new, 197 total green), golden pipeline green, committed + pushed. Key learning: a plural-vs-singular naming mismatch between `strings.xml` (`category_caps_channel_*`) and `Application.kt` code references (`category_cap_channel_*`) caused "Unresolved reference" compile errors — the string existed in the XML but never linked to Kotlin due to the naming discrepancy. Fixed by aligning code to the pluralized resource names. EN-only strings (RU file validated as proper UTF-8 in security audit).
- **2026-08-10 Security audit + de-link (committed `29f4ad8`)**: full security review (rules now in `.track/SECURITY.md`, binding on every agent — AGENTS.md §8.1 + package map updated). Removed ALL hardcoded external links/contacts: `About.kt` developer link (`danilkinkin.com`) + "Contribute" buttons (`buckwheat.app`); deleted `BugReporter.kt` (GitHub issues + `hello@danilkinkin.com`), `CatchAndSendCrashReport.kt` (crash-email), `util/openIn.kt`, `util/collectEnvInfo.kt`; removed `BUG_REPORTER_SHEET` + crash snackbar; `About` = title + description only; `CrashLogger` kept (local-only, Downloads). Manifest hardening: `fullBackupContent`/`dataExtractionRules` exclude `file: datastore/settings.preferences_pb` so the Voice AI API key never leaves via cloud backup/transfer. Only outbound endpoint = user-chosen Voice AI provider (default OpenRouter); zero analytics/ads/tracking SDKs; permissions unchanged; package id kept. **Corrected false belief**: `values-ru/strings.xml` is VALID UTF-8 Cyrillic (byte check `D0 93 D1 80` = `Гречка`; strict decode 0 replacement chars) — the earlier "corrupted mojibake repo-wide" claim was a PowerShell 5.1 console/ANSI display artifact, NOT real corruption. Any agent must verify encodings by bytes, never trust garbled console output. Pipeline green: **187 tests + assembleDebug**.
- **2026-08-10 Voice widget per-instance config (Feature 8, committed `e26dca5`)**: `android:configure` on `VoiceWidgetReceiver` launches a new `VoiceWidgetConfigureActivity` at placement; it picks "Follow global design" (clears the per-instance override) or one of the 4 designs for that widget instance and writes it via `updateAppWidgetState(context, AppWidgetId(appWidgetId))` into the instance's own Glance state (verified Glance 1.1.1: state is keyed purely by `appWidgetId`, `AppWidgetId(widgetId)` constructor is public, and `getGlanceIdBy` does NOT exist — so the write is by constructed `AppWidgetId`, not a manager lookup). `CommonWidgetReceiver.observeData` now resolves each instance's **effective** design as `voiceDesignOverridePreferenceKey ?: global` instead of clobbering every instance with the global value; `VoiceWidgetContent` still reads `voiceDesignPreferenceKey`. Pure `effectiveVoiceWidgetDesign` + 4 tests → **187 tests green + assembleDebug**. Strings are EN-only (the existing `voice_widget_design_*` strings are EN-only; the "RU file corrupted" reason recorded at the time was wrong — see security-audit entry above). E2E placement needs a real launcher — unit tests only cover the pure helper.
- **2026-08-10 Category drill-down analytics (Feature 7, committed `372b3c5`)**: tapping a category chip in the analytics breakdown (`SpendCategoriesCard`) opens `CATEGORY_HISTORY_SHEET` → `ViewerHistory(onlyCategoryKey = …)` showing that category's transactions for the period, filtered in `History.composeHistoryRows` via the new pure `transactionMatchesCategory(tx, key)` (archived rows still only join when searching, matching the `onlyDay` behavior). `TagAmount` gained a clickable-`Surface` `onClick`; the card now keeps `Pair<CategoryKey, TagUsage>` per chip and forwards `onCategoryClick`. Header shows the category (`🍔 Food` / custom name). No new strings. `SpendCategorizerTest` +3 (match/mismatch cases) → **183 tests green + assembleDebug**.
- **2026-08-10 Weekly/monthly spend digest (committed `1314350`, pushed)**: Settings toggle (`SpendDigestSetting`, after `RecurringPaymentAlertSetting`) + Frequency dialog (Weekly/Last 7 days, Monthly/Last 30 days) + `TimePickerDialog` (default 20:00) posts a digest notification (channel `spend_digest`, id 105) summarizing the period's SPENT transactions. Pure logic in `notifications/SpendDigestContent.kt` (`digestRange`, `buildSpendDigest` sums/filters/averages, `buildSpendDigestMessage`); one-shot `setWindow` + re-arm in `SpendDigestScheduler`/`SpendDigestReceiver` (`@AndroidEntryPoint`, injected `TransactionDao.getAll(SPENT, from, to)`, re-arms +7d / +1 calendar month). Boot reschedule in `ReminderBootReceiver`. 7 `SpendDigestContentTest` tests. **180 tests green + assembleDebug.**
- **2026-08-10 Recurring-payment due alerts (committed `31e02b7`)**: Settings toggle (`RecurringPaymentAlertSetting`, after the daily budget reminder) fires a notification at a chosen time (default 09:00, `TimePickerDialog`) listing recurring templates due today (Room `getDueOnDay(today.dayOfMonth)`), channel `recurring_due` (id 103, one-shot `setWindow` + re-arm next day, boot reschedule in `ReminderBootReceiver`). Pure text builder extracted to `notifications/RecurringDueContent.kt` (`buildRecurringDueText`, trims `numberFormat` trailing space) → 4 Robolectric tests. **167 tests green + assembleDebug.**
- **2026-08-10 End-of-period spend forecast (committed `b898654`)**: `wallet/BudgetSummary.kt` shows a forecast card (`SpendForecastCard`, `ic_equalizer`) — "At this pace you'll use ~X% of budget (₹Y) by <finish>" from `wallet/SpendForecast.kt` (`forecastEndOfPeriodSpend`, daily-average linear projection, clamps today to finishDate, null-guarded). 6 `SpendForecastTest` cases (one expected-value correction: overspend projection 413.3%, not my initial 160.0).
- **2026-08-10 Round values setting (committed `89840b5`)**: port of upstream draft `79c0c58` modernized to DataStore (`roundValuesStoreKey`) + `util/NumberDisplayConfig.kt` volatile cache collected in `Application.kt`; `numberFormat(applyRoundValues=true)` forces whole numbers everywhere except live typed inputs (`visualTransformationAsCurrency` passes `applyRoundValues=false`). 4 `NumberFormatTest` cases.
- **2026-08-10 8-feature roadmap selected (user picked ALL)**: (1) round values ✅ `89840b5`, (2) forecast end-of-period spend ✅ `b898654`, (3) recurring-payment due alerts ✅ `31e02b7`, (4) savings-goal progress nudges ✅ `8f04f0e`, (5) weekly/monthly spend digest ✅ `1314350`, (6) AI monthly report share — **DROPPED by user decision** ("something other with no ai" → "any other feature"; no code written), (7) category drill-down analytics ✅ `372b3c5`, (8) voice widget per-instance config ✅ `e26dca5`. Each got the golden pipeline + commit + push. **Roadmap complete (7/7 built).**
- **2026-08-09 Daily budget reminder ported to `setWindow` (committed `96feeec`, pushed)**: the daily reminder was the last remaining `setRepeating` alarm (the ~18h inexact window the widget refresh fixed on 08-08). `DailyBudgetReminderScheduler.schedule()` now uses `alarmManager.setWindow(RTC_WAKEUP, trigger, WINDOW_MILLIS = 10 min, pi)`; `DailyBudgetReminderReceiver` posts the notification then `rearmNextDay(context)` reads the stored `reminderHour`/`reminderMinute` (defaults 20:00) and re-schedules for the next day. `ReminderBootReceiver`/`BackupRepository`/`DailyBudgetReminderSetting` unchanged. **Follow-up noted**: `OnTrackAlertScheduler` still uses `setRepeating` (same flaw, out of scope).
- **2026-08-09 AI-voice root cause FIXED (committed `96feeec`, pushed)**: live OpenRouter listing (2026-08-09) shows `nvidia/nemotron-3-ultra-550b-a55b:free` is NOT dead (still listed) — it's a 550B ultra model whose free tier is heavily rate-limited → the surfaced failure was `HTTP 429`/availability, not a 404. Fix: default swapped to **`openai/gpt-oss-20b:free`** (20B, structured-output-tuned, first pick of the settings sheet's live "fastest-first" dropdown). Defaults centralized in `di/SettingsRepository.kt`: `DEFAULT_VOICE_AI_PROVIDER_URL`, `DEFAULT_VOICE_AI_MODEL`, plus `normalizeVoiceAiModel(saved)` which maps the legacy ultra string → new default so **already-saved settings auto-upgrade** on the user's device (no sheet visit needed). All four consumers updated: `SettingsRepository.getVoiceAiProviderUrl/getVoiceAiModel`, `keyboard/VoiceAi.kt`, `settings/VoiceAiSettingsSheet.kt`, `settings/VoiceAiSettingsViewModel.kt`, `data/categories/SpendCategorizer.kt`. This also closes the "two sources of truth" duplication decision (below). 153 tests green. **Next**: user re-runs voice input on the device with this build — `HTTP 429` should no longer appear; if any failure surfaces now it's a genuine model/key problem and the widget caption shows it.
- **2026-08-09 Voice widget 4th design: Graph background (committed `053dbc2`, pushed)**: new `VoiceWidgetDesign.GRAPH_BG` option in the widget design switcher (Settings row + dialog, strings `voice_widget_design_graph_bg`/`_desc`). The widget surface is a pastel status tint (green `C9E7C5` → amber `FFE3B0` → red `FFC9C6`, blended by `1 - remainingFraction` via `statusColor`) with the spend chart drawn full-bleed in deep tints (`statusDeepColor`: `4C8B48`→`D99A2B`→`D5534C`) by new `drawStatusBackgroundBitmap` (300×120dp: filled bezier area @35%, dashed goal line @55%, ring+dot today marker in text color `2A2A2A`). Foreground text is fixed dark (`2A2A2A`/`464646`, ADDED caption `1B7A2F`). Hero = remaining percent (`20sp` bold). Chart is only rendered into the bitmap when `isGraphBg && stateSet`, else the static preview pill stays (preserves the 16dp-corner fix from `cedc0e0` — still first-child `Image`, no `background(ImageProvider)` modifier); the mid-column `VoiceWidgetChart` is skipped for GRAPH_BG. `remainingFraction`/`remainingPercent` hoisted above the design `when` to share one computation. **On-emulator (API 36) verified + pushed**: fresh APK installed, design switched in Settings → home, waited for async re-render. Pixel scan of the 5×2 widget band confirmed: base `201,231,197` = StatusGreen exactly (remainingFraction = 1.0, nothing spent today, dailyBudget `160.97` = budget/32 days), chart fill `158,199,153` = `4C8B48`@35% over base, dashed goal line at the top (y=577, `133,180,129` = deep-green @55%, 8px dash / 13px gap pattern), dark hero "100%" + caption/sub/summary in `2A2A2A`/`464646`, mic button primary blue `73,93,146`. Regression: switched to Percent left → pastel green fully gone (0 pixels), static pill restored; switched back to Graph background.
- **2026-08-09 AI-voice diagnosis (COMPLETED — see root-cause fix above)**: user reported voice input "parses wrong, no AI"; has an API key configured and uses the **widget mic**. Step 1 (`dbee831`, pushed) shipped `ParsedTranscript(results, aiFailure)` so the widget notification/caption now append ` · AI off: <reason>` and both widget + in-app branches `Log.w("VoiceAI", …)`. `NotConfigured` intentionally stays silent. `parseVoiceAiContents` prose-fallback is tested behavior (`VoiceInputParserTest.kt:244,377`) — kept. The diagnosis payload surfaced the reason; root cause then fixed in `96feeec` (default model → `gpt-oss-20b:free`).
- **2026-08-08 Midnight widget refresh (committed `81b7ab2`, pushed)**: new `widget/WidgetRefreshScheduler.kt` + `widget/WidgetRefreshReceiver.kt`. Scheduled daily at **00:05** via `alarmManager.setWindow(RTC_WAKEUP, trigger, 10 min, pi)` — **NOT `setRepeating`**, which is inexact on modern Android with a ~+18h window (observed in `dumpsys alarm`) and would defer the refresh far past midnight; `setWindow` bounds the delay to ≤10 min without needing `SCHEDULE_EXACT_ALARM` (denied by default on API 33+). One-shot + re-arm: `onReceive` calls the three `WidgetReceiver.requestUpdateData(...)` (same path as `Application.onActivityPaused`) then `schedule()` again. Wired in `Application.onCreate` + `ReminderBootReceiver` (BOOT_COMPLETED reschedule). Receiver is `exported="false"` (alarm delivers in-app via same UID). Deliberately a re-read + re-render ONLY — does NOT replicate `SpendsViewModel.runChangeDayAction`'s daily budget fold (ASK mode needs a UI dialog; fold is `changeDayMutex`-guarded). Verified end-to-end on the emulator (temp +90s trigger): onReceive → 3 updateAction broadcasts → Glance SessionWorker re-render → re-arm; final `dumpsys alarm` shows `2026-08-09 00:05:00 window=+10m`. **Pitfall recorded**: shell `am broadcast` CANNOT deliver to `exported="false"` receivers (silent `result=0`, no onReceive) — to test the chain the receiver was temporarily `exported="true"` then reverted. The pre-existing `DailyBudgetReminderScheduler` shares the `setRepeating` huge-window flaw — left untouched (out of scope, likely a follow-up fix). **153 tests green.**
- **2026-08-08 Analytics "vs previous" fix (committed `855e240`, pushed)**: the compare card / trend delta intermittently vanished because both `CompareToLastPeriodCard.findPreviousPeriod` and `SpendsTrendCard.previousPeriodBefore` required `finishDate.before(roundToDay(currentStart))`, but `finishDate` is stored at end-of-day (`roundToDay(finish) + DAY - 1000`) — so a previous period finished early on the same day the current period starts, or a mid-period restart archiving the old period with a still-future scheduled finish, never qualified. Fix: `CompareToLastPeriodCard.effectiveFinishDate(period) = actualFinishDate ?: finishDate`, and both comparisons use day-granularity `!effectiveEnd.toLocalDate().isAfter(startDay)`. 7 new regression tests → **153 green** (was 146). Known limitation: card still hidden when current period has zero spends (`Analytics.kt:143` gates on `spends.isNotEmpty()`).
- **2026-08-08 Voice widget 16dp corners + app-pause refresh (committed `cedc0e0`, pushed)**: `background(ImageProvider(...))` on the outer Box triggers Glance 1.1.1's `transformBackgroundImageAndActionRipple` (`NormalizeCompositionTree`) which hoists `cornerRadius` off the `@android:id/background` view → Launcher3 `RoundedCornerEnforcement` applied ~23dp. Fixed by making the pill an explicit first-child `Image(provider, contentScale = FillBounds, contentDescription = null)` (no `background` modifier). Pixel-verified all four corners at exactly r=48px (16dp). Same commit: `Application.onActivityPaused` now calls the three widget receivers' `requestUpdateData` so widgets pick up new spends on app close.
- **2026-08-07 Voice widget design switcher (committed `9432172`, pushed)**: Settings gained a "Voice widget design" row (`ic_widgets`, end caption = current design) opening a sheet with three `CheckedRow` options — **Percent left** (default), **Amount left**, **Ring** — backed by `SettingsRepository.voiceWidgetDesignStoreKey` + `getVoiceWidgetDesign()` and `settings/VoiceWidgetDesignSetting.kt` (`VoiceWidgetDesignViewModel`, `switchVoiceWidgetDesign(context, design)` writes the datastore then `requestUpdateData`). `CommonWidgetReceiver.observeData` writes the design name under the new `voiceDesignPreferenceKey`; the `VoiceWidget` layout branches `when (widgetDesign)` (PERCENT/AMOUNT/RING). The ring is a bitmap (`drawRingBitmap(context, 48dp, fraction, percentText, ringColor, trackColor, textColor)`) like the chart/bar — **`toColorProvider()` is `@Composable`, so the pure draw fn loads Manrope wght 900 via `Typeface.Builder(assets, "font/manrope_variable.ttf").setFontVariationSettings("'wght' 900")` instead of `applyFontToPaint`**. On-device (API 36) E2E: row + dialog + datastore persistence verified; widget re-renders **async (~2-8s after switch)** — a screenshot 2s after HOME was stale, 6-8s was correct. Pixel-verified all three: PERCENT (bar y772-778 full width), AMOUNT (single hero amount line, no bar/"of daily"), RING (full ring at ~100%, primary arc x≈401-537 y≈650-780, chart + summary below).
- **2026-08-07 Voice widget: big-percent design with budget progress bar (committed `75e12c2`, pushed)**: the widget hero is now the **% of the daily budget remaining** (integer, primary 18sp) under the "Left today" caption, plus an "of ₹X daily" sub-line (`voice_widget_of_daily`), a thin 3dp rounded progress bar filled to the remaining fraction, the unchanged min/max-spike chart and the Budget/Spent/Left summary. **Glance 1.1.1 `defaultWeight()` takes no weight arg** ("Too many arguments") — a weighted two-Box Row won't compile, so the bar is drawn into a bitmap (`drawProgressBarBitmap(context, 280dp×3dp, fraction, fillColor, trackColor)` → drawRoundRect track + fill) and stretched via `ContentScale.FillBounds`, same as the chart. On-device (API 36) pixel-verified: caption y622-642, hero y672-702, "of ₹X daily" y732-764, bar y772-778 fill x398-1039, chart y791-866 (red max ≈(398,791), blue min ≈(713,842), today dot ≈(1040,859)), summary y858-903.
- **2026-08-07 Voice widget redesign: daily remaining + min/max markers + summary (committed `492d7c8`, pushed)**: per user request ("graph shows spend as spikes, 2 points for min and max, the daily point = remaining budget, plus daily budget and daily remaining and the SPEND entry") the widget now: caption **"Left today"**; big amount = **daily remaining** `(dailyBudget − series.last()).coerceAtLeast(0)` (series last element is today's spend); the curve chart with **range markers** for the min/max days (past days only — max red ring `topColor`, min blue ring `bottomColor`, white center, drawn before the today marker); and one summary row **"Budget ₹X · Spent ₹Y · Left ₹Z"** (`voice_widget_today_summary`). Removed `compactAmount` + `voice_widget_chart_min/max` strings. Renamed `drawRangeMarker`'s `color` param → `rangeColor` (name collided with `Paint.color` inside `apply { color = … }`, caused "val cannot be reassigned"). On-device pixel-verified: layout y632-652/682-712/742/842-852, red max ring ≈(398,745)-(411,769), blue min ring ≈(713,796)-(730,817), today white dot ≈x1045.
- **2026-08-07 Voice widget: min/max-style spend chart (committed `8c4ead0`, pushed)**: the widget chart now looks like the analytics min/max spend chart (`SpendsChart`): a smooth cubic-bezier filled area with a vertical `colorMax→colorMin` gradient (night-mode alphas 0.55/0.38/0.20 vs light 0.35/0.20/0.08), a **dashed** goal-guard line at the daily-budget level, and a "today" marker (primary ring + halo + white dot) on the last point. Key constraint discovered: **Glance 1.1.1 has no canvas/path composables** (`androidx.glance.canvas` isn't in the 1.1.1 jars) and `ContentScale` is only `{ Fit, Crop, FillBounds }` — so the chart is drawn into an `android.graphics.Bitmap` at composition time (the `CanvasText` technique) and rendered with `ContentScale.FillBounds`. `drawChartBitmap(context, designWidthDp=280, chartHeightDp=26, series, dailyBudget, topColor=colorMax, bottomColor=colorMin, todayColor=primary, goalLineColor, markerColor)` is a pure pixel-drawing function. Verified on-device via pixel scans (goal line y744, peak x402-555 y758-818, marker ≈(1040,812)); 146 tests green.
- **2026-08-07 Voice widget: allow adding another spend after a commit (committed `e8162a2`, pushed)**: after a successful voice commit the widget was stuck on the ADDED green check (no action, no auto-reset), so the user couldn't add a second spend without opening the app. Fix has two parts: (1) `AddedButton` is now **clickable** — `actionRunCallback<VoiceWidgetMicCallback>()` starts a fresh voice input immediately; (2) new `scheduleFeedbackReset(context, delayMs=4000)` (VoiceWidget.kt) runs on a process-lifetime `CoroutineScope(SupervisorJob() + Dispatchers.Main)` and resets `voice-feedback-state-key` ADDED → IDLE only while the state is still ADDED (never clobbers a session the user already started). Called from `VoiceWidgetCommitService.commit()` right after `setVoiceFeedbackState(ADDED, text)`. Deliberately NOT on the service scope — the service stops immediately and an in-flight `handling` flag would drop a quick re-tap; process-level one-shot is best-effort, tappable check covers the worst case. **Verify**: compile/spotless/146 tests/assembleDebug green; emulator regression re-verified (IDLE render + mic tap → LISTENING pixel-checked). ADDED visuals + reset need real-device confirm.
- **2026-08-07 Voice widget: spend chart + live input feedback (committed `8c2ba4f`, pushed)**: the widget renders a real bar chart of the last 7 days of spend (clamped to the period start) with a goal-guard line at the budget level, today's bar + white commit marker, min/max captions, and state-driven mic feedback (IDLE mic / LISTENING+PROCESSING ring + `ic_equalizer` / ADDED `colorGood` circle + white check + amount caption). Series + daily budget stored under `voice-chart-series-key` / `voice-daily-budget-key` in the shared `CommonWidgetReceiver` state block; feedback under `voice-feedback-state-key` / `voice-feedback-text-key` (set by `setVoiceFeedbackState(...)` in VoiceWidget.kt, called from the mic callback and the commit service). `TryWidget` gained the voice row. On-device pixel + logcat verification details in `CHANGELOG.md` head. **ADDED/PROCESSING visuals can't run on the emulator** (no speech service) — unit-tested instead.
- **Voice widget notification-permission fix (committed `e2370e3`, pushed)**: `POST_NOTIFICATIONS` was never requested on the widget tap path, so "permission needed" / "Spend added" / error notifications were silently dropped on API 33+ (and the mic-missing notice was *itself* a notification — invisible when notifications are denied). Fix: `VoiceWidgetMicCallback.onAction` now launches `MainActivity` directly (with `EXTRA_REQUEST_MIC_PERMISSION`) when `RECORD_AUDIO` is missing, and `MainActivity`'s launcher switched to `RequestMultiplePermissions()` to request `RECORD_AUDIO` + `POST_NOTIFICATIONS` (API 33+) together. On-device verified with BOTH permissions revoked: mic tap → MainActivity opens → both system prompts appear in order → after granting, mic tap starts the FGS (`allowWiu:52`) and the result notification (`id=301`, `channel=voice_widget`) displays.
- **Voice widget "can't show content" — FULLY FIXED, second root cause found via on-device emulator E2E (committed `abe131c`, pushed)**: the WorkManager init fix (`7d32713`) made `provideGlance` actually run, unmasking a second bug — `CanvasText` → `Color.toColorProvider()` (CommonWidgetReceiver.kt:29) reflectively instantiates Glance `ColorProvider`, which is an **interface**, so the `catch` falls back to `LocalContentColor.current` = `compositionLocalOf { throw Error("No set") }`. Minimal/Extend widgets provide it via `CompositionLocalProvider`; VoiceWidget didn't → `java.lang.Error: No set` → "Can't show content". Fixed by wrapping `VoiceWidgetContent` in the same provider (VoiceWidget.kt). Verified on emulator: widget renders (pixel + uiautomator, no error text). **`VoiceWidgetRenderTest.rendersWithoutCompositionException` was a false-green** (Robolectric `widget.update()` never runs `provideGlance`) — removed; on-device E2E is authoritative. User confirmed the widget works on their device.
- **2026-08-07 all feature work committed AND pushed on `master` @ `8c2ba4f` (working tree clean)**: voice widget feature + fixes (`1fa1534`, `8a7fb2a`, `9c2f0ee`, `b80336f`, `7d32713`, `abe131c`, `e2370e3`), review-wave fixes (`8dce722`, `18926d5` — details in `CHANGELOG.md`), voice AI free-model dropdown (`8a6c55d`), proactive on-track overspend alert (`cb6f72f`), plus the earlier wave (`d389a0e`, `9a75b05`, `e7249c9`, `3bbdd9c`, `a147c2d`). **146 tests green (14 suites)**; `compileDebugKotlin` + `assembleDebug` + `spotlessCheck` + `testDebugUnitTest` all pass. Decision flips from the review wave: restore is now transactional (`BackupRepository` gained a `DatabaseModule` constructor param — `BackupRepositoryTest` builds an in-memory Room DB); the Voice-AI API key is deliberately excluded from backup exports; `overspendNotifiedStoreKey` is a "currently over" mirror that every mutation of today's `spentFromDailyBudget`/`dailyBudget` must keep in sync inside the same `edit {}` block.
- **2026-08-07 Day-of-week spend breakdown (committed `a147c2d`, pushed)**: new `analytics/SpendsWeekdayCard.kt` — bar chart of average daily spend per weekday for the elapsed part of the current period, styled like `SpendsTrendCard` (gradient `colorMax→colorMin` bars scaled to max weekday average, tap-to-highlight + "Mon · ₹120" caption in primary, weekday labels via `getWeek()`/`prettyWeekDay`). Pure `weekdayAverageSpend(spends, startDate, finishDate, today)` returns 7 scale-2 values (Mon=0..Sun=6, `DayOfWeek.value - 1`), averaging each weekday total by its occurrence count within `startDate..min(today, finishDate)`; future weekdays → 0, out-of-window spends ignored. Inserted in Analytics between `SpendsTrendCard` and `CompareToLastPeriodCard`; new string `weekday_breakdown_title`. 7 new `SpendsWeekdayTest` cases → 131 tests green. Verified: `compileDebugKotlin`, `testDebugUnitTest` (131), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-07 Share spending summary + compare to last period + overspend notification (committed `e7249c9`, `3bbdd9c`)**: Analytics gained a "Share summary" ButtonRow (new `ic_share`) opening the system share sheet with a text summary (period dates, budget, spent, remaining clamped ≥ 0, daily average, transaction count, category breakdown); `analytics/ShareSummary.kt` with pure `buildShareSummary(...)` + `rememberShareSummary(...)`. Also `CompareToLastPeriodCard` (this period vs previous at same elapsed days, `findPreviousPeriod`/`previousSpentAtSameElapsedDays`) and the instant `OverspendingNotifier` (fires once when a spend crosses the daily budget, opt-in `OverspendNotificationSetting`)
- **2026-08-07 Emoji per category (committed `d389a0e`, pushed)**: `SavedCategory.emoji` column (default `''`) + DB `v12 → v13` (`AutoMigration12to13`, schema `13.json`); `SpendCategory` gains per-enum `emoji` + companion `DEFAULT_EMOJI`/`fromStored`/`emojiFor`; new `base/EmojiPicker.kt` (`CATEGORY_EMOJI_OPTIONS` chip row); wired into Categories Management sheet (add/edit rows), editor `CategorySelector`, and analytics category cards. 111 tests green
- **2026-08-07 Full JSON backup & restore (committed `9a75b05`, pushed)**: new `backup/BackupData.kt` (pure codec: `BackupData`, sealed `BackupValue` with typed pref values, `toJsonString()`/`parseBackupData()`, `BACKUP_VERSION = 1`, app-tag guarded) covers every Room entity (transactions incl. category, budget periods incl. isImported, archived transactions, saved tags, saved categories, recurring templates, savings goals) plus both DataStore preference maps (budget + settings) via `asBackupMap()`/`applyBackupMap()`. DAOs gained `getAllNow()`/`insertAll()`/`deleteAll()`. New Hilt `di/BackupRepository.kt`: `exportBackup()` snapshots everything; `restoreBackup(json)` wipes current data and reinserts FK-safe (periods → archived → transactions → tags → categories → recurring → goals), preserving ids so `archived_transactions.period_id` links survive, then `clear()`s + re-applies both DataStores. UI: `settings/BackupRestoreSetting.kt` (two TextRows in Settings after Import CSV: "Back up data" via `CreateDocument`, "Restore backup" via `OpenDocument` + `AlertDialog` confirmation) + `settings/BackupRestoreViewModel.kt`; new strings `backup_*`/`restore_*` + `ic_file_upload`. Decisions: restore replaces all current data (confirmation dialog required); restore NOT wrapped in one DB transaction (matches existing `SpendsRepository` multi-DAO pattern — partial failure recoverable by re-restore); ids preserved for FK integrity. 11 new tests (8 `BackupDataTest`, 3 `BackupRepositoryTest` with new `FakeRecurringDao`/`FakeSavingsGoalDao`, updated existing fakes) → 111 tests total
- `master` is **our fork** (`skcode98/buckwheat`, based on upstream master @ `4b60102`) with the implemented feature set below
- **2026-08-06 Custom categories now show in the Analytics breakdown (committed `d63f670`, pushed)**: closes the documented gap from the categories feature — `SpendCategoriesCard` previously ran every spend through `categoryFor()` (enum-only), so spends manually assigned a custom category fell under keyword/OTHER. New pure helpers in `SpendCategorizer.kt`: `CategoryKey` (sealed: `BuiltIn(SpendCategory)` | `Custom(name)`), `categoryKey(tx)` (persisted built-in → enum; any other non-blank persisted value → `Custom` with the raw name, even if a keyword matches — persisted value wins; blank/null → offline keyword enum), and `categoryTotals(spends)` (aggregates into (key, total), drops non-positive). The card maps buckets → `TagUsage`: built-ins get their localized label + `colors[ordinal]` (OTHER → restColor/special), customs get the raw name + `colors[floorMod(name.hashCode, size)]` (stable per name). Old `categoryFor` kept intact (still tested). 6 new `SpendCategorizerTest` cases (`categoryKey` precedence/custom/blank, `categoryTotals` merge + drop) → 100 tests total. Verified: `compileDebugKotlin`, `testDebugUnitTest` (100), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Categories management in Settings + editor category picker reworked (committed `8f6878e`, pushed)**: user asked for category management "like tag" and reported the editor's M3 `DropdownMenu` category picker didn't match the app UI. (1) New Room entity `SavedCategory` + `SavedCategoryDao` (`saved_categories`, unique `name` index) via manual migration 11→12 + DB version 12 (mirrors the `saved_tags` migration 8→9 pattern; added to `MANUAL_MIGRATIONS`, `AppModule` provider, `SpendsRepository` constructor now takes `savedCategoryDao`). (2) `SpendsRepository.getAllCategories()` merges distinct non-null `transactions.category` values (current period only — archived records have no category column) with saved custom names; test fakes got a `FakeSavedCategoryDao`, `SpendsRepositoryTest` constructor updated, new `getAllCategoriesMergesTransactionCategories` test (94 total). (3) `CategoriesManagementViewModel` exposes `CategoryItem(name, id)` where `id == null` = built-in (from the 8 `SpendCategory` enum entries) or transaction-only residue; add/rename/delete reject blanks and built-in names (`SpendCategory.fromStored` guard); `CategoriesManagementSheet` mirrors `TagsManagementSheet` (add field + rows; built-ins read-only with "Built-in" caption, saved ones get edit/delete, transaction-only ones get an add affordance to re-save). Settings gained a Categories `TextRow` (`ic_label`) opening `CATEGORIES_MANAGEMENT_SHEET` (registered in `BottomSheets`). (4) `EditorViewModel.currentCategory` generalized from `MutableLiveData<SpendCategory?>` to `MutableLiveData<String?>` (prefill = `transaction.category` directly; `Keyboard.kt` 4 commit sites changed `.value?.name` → `.value`). (5) `CategorySelector` pill now opens a new app-styled `CategorySelectorSheet` (bottom sheet with `CheckedRow`s: Auto + built-ins + saved customs; currently-selected category prepended if it no longer exists in the managed list) instead of the Material3 `DropdownMenu`; new shared `categoryDisplayName(stored)` helper (built-in → localized `labelRes`, custom → raw name). New strings `categories_management_*`, `category_selector_title`. **Known limitation (documented, not fixed):** custom categories assigned manually are not aggregated in the analytics `SpendCategoriesCard` — `categoryFor()` only knows the 8 built-ins, so custom-named spends fall back to keyword/OTHER there. Verified: `compileDebugKotlin`, `testDebugUnitTest` (94), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Currency: curated list + INR default (committed `8f6878e`, pushed)**: user wants only a few important currencies and Rupee as default. `getCurrencies()` (WorldCurrencyChooser) now returns a curated 12-code list, INR first: INR, USD, EUR, GBP, JPY, CNY, AED, AUD, CAD, CHF, SAR, NZD (previously `Currency.getAvailableCurrencies()` — ~160 entries). The chooser prepends the currently-selected currency when it's outside the shortlist (`remember(defaultCurrency)`), so existing picks are preserved. `SpendsRepository.getCurrency()` now defaults to `ExtendCurrency.getInstance("INR")` when `currencyStoreKey` is absent (was NONE). Decisions: explicit "No currency" still stores `""` → NONE (unchanged); archived periods already persist the code via `currentCurrencyCode()` so they'll record INR; display everywhere uses `spendsViewModel.currency` → now ₹ by default on fresh installs. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Analytics calendar day-tap + heatmap scope (committed `756be1f`, pushed)**: (1) Tapping a spend day on `SpendsCalendar` previously reset the editor + `onClose()` (dumped to home). Now it opens `VIEWER_HISTORY_SHEET` filtered to that day — `History(onlyDay: LocalDate?)` filters in `composeHistoryRows` before grouping, `ViewerHistory` shows the date in the header via `prettyDate`, `BottomSheets` passes `state.args["onlyDay"] as LocalDate?`, `Analytics.onDayClick` pushes `PathState(name = VIEWER_HISTORY_SHEET, args = mapOf("onlyDay" to date))` (needs `com.danilkinkin.buckwheat.data.PathState` import). Viewer closes back to analytics. Removed `editorViewModel` param from `Analytics` + unused `toDate` import. (2) Heatmap month nav is clamped to the spend period: `currentMonth = YearMonth.now().coerceIn(firstPeriodMonth, lastPeriodMonth)` with arrows disabled at the bounds — no more scrolling into unrelated months. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Period picker calendar limited to current + next month (committed `756be1f`, pushed)**: user reported the onboarding period calendar showed ~2 years. Root cause: `FinishDateSelector` set only `disableBeforeDate` (now − 1 month); `CalendarState.listMonths` renders from `disableBeforeDate` month to `disableAfterDate` month, defaulting the upper bound to Dec 31 of year + 2 → ~27 months. Fix: `FinishDateSelector` now passes `disableBeforeDate = LocalDate.now().withDayOfMonth(1)` and `disableAfterDate = last day of next month` (now.plusMonths(1).withDayOfMonth(1).minusDays(1)), rendering exactly current + next month and bounding selection to that range. NOTE: this overrides the earlier "period may start up to one month back" decision — a fresh period can now start on the 1st of the current month at the earliest. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Trend chart polish on the monthly trend card (committed `d4ec813`, pushed)**: `SpendsTrendCard` now (1) taps a bar to highlight it + show a detail caption ("4 Aug · ₹150") above the info row, (2) draws a dashed average-spend line + `avg/day` caption in the info row (`avg_per_day` string), (3) shows month labels under the bars when the period spans multiple months (weighted by days per month; "MMM" for current year, "MMM ''yy" otherwise). New pure `monthDayCounts(start, finish)` → `List<Pair<YearMonth, Int>>` — counts the period's days per calendar month (first month starts at `start`, not the month's 1st — that bug produced 31 instead of 12 for Jan-20→Feb-10); `shortMonthLabel` formats via `LocalConfiguration.current.locales[0]`. `SpendsTrendBars` grew `averageDaily`, `selectedDay`, `highlightColor`, `averageLineColor`, `onDayTap` (tap index = `floor(x / (width / n))` clamped). 4 new `SpendsTrendTest` cases (`monthDayCounts*`). Verified: `compileDebugKotlin`, `testDebugUnitTest` (93 tests), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Category picker reworked into a compact dropdown (committed `d4ec813`, pushed)**: user said the editor looks complicated with the category chip row. `CategorySelector` is now a single collapsed pill ("Auto" + `ic_label` + `ic_arrow_down`, 36 dp, left-aligned, `padding(horizontal=24.dp)`) that opens a `DropdownMenu` (Material3 1.3.2) listing "Auto" + the 8 `SpendCategory` entries; the selected item is highlighted in `primary`. Assignment stays opt-in (only visible when opened); commit wiring (`EditorViewModel.currentCategory`, `Keyboard.kt`) unchanged. New string `category_auto`. Verified: `compileDebugKotlin`, `testDebugUnitTest`, `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Monthly trend card in Analytics (committed `eff40c0`, pushed)**: new `analytics/SpendsTrendCard.kt` — Canvas per-day bar chart of the current period's spends (bar per day-of-period, scaled to max daily, `colorMax`→`colorMin` gradient, zero days get a 1% baseline nub) + header with the period total and a "vs previous period" +/−% delta (green/red) when a previous real period exists. Data: new `SpendsRepository.getAllBudgetPeriods()` → `SpendsViewModel.budgetPeriods`; pure `previousPeriodBefore(periods, start)` = latest non-imported `BudgetPeriod` finishing before `roundToDay(start)`; pure `dailySpendTotals(spends, start, finish)` (guard: `roundToDay(tx).before(start)` excluded — naive `countDays(tx,start)-1` alone maps a pre-period spend onto a positive day index). Inserted after `CategoriesChartCard` in `Analytics.kt`; strings `monthly_trend_title`, `vs_previous_period`. Decisions: comparison targets real archived periods only (CSV month buckets `isImported=true` are excluded as artifacts); zero-spend previous period hides the delta (no division); delta = `(total/prev·100 − 100)`, formatted via `NumberFormat`, `+`/`−` prefixed. Verified: `compileDebugKotlin`, `testDebugUnitTest` (89 tests incl. 6 new `SpendsTrendTest`), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Manual spend category in the editor (committed; superseded by the dropdown `d4ec813`)**: chip row (`editor/category/CategorySelector.kt`) in the editor assigns one of the 8 predefined `SpendCategory`s to the record being added/edited. `EditorViewModel.currentCategory` (LiveData<SpendCategory?>) prefilled from `transaction.category` in `startEditingSpent`, reset to null on create/reset; ADD and voice commits set `category = currentCategory?.name`, EDIT commits via `copy`. Tapping the selected chip deselects → null (auto). Decision: manual pick must take precedence over AI — confirmed the AI pass only fills `category.isNullOrBlank()` rows, so manual assignments are never overwritten; null default keeps offline/AI categorization for unassigned records. No DB change. Verified: `compileDebugKotlin`, `testDebugUnitTest` (green), `assembleDebug` BUILD SUCCESSFUL
- **2026-08-06 Daily budget reminder notification (committed `0a76ba1`, pushed)**: optional daily notification with the budget left for today. New `notifications/` package — `DailyBudgetReminderScheduler` (AlarmManager `setRepeating`, RTC_WAKEUP, INTERVAL_DAY, default 20:00; `cancel()` on toggle-off), `DailyBudgetReminderReceiver` (`goAsync()` + IO coroutine reads `budgetDataStore` dailyBudget/spentFromDailyBudget/currency, posts NotificationCompat on channel `daily_budget_reminder`), `DailyReminderContent.buildDailyReminderMessage` (pure: LEFT "You have X left today" / OVER "Daily budget spent — X over" abs / NO_BUDGET hint), `ReminderBootReceiver` (BOOT_COMPLETED reschedule). Settings gets a Switch row + time row (`TimePickerDialog`); enabling on API 33+ requests `POST_NOTIFICATIONS` and only activates if granted; persisted via `SettingsRepository` keys `reminderEnabled`/`reminderHour`/`reminderMinute`. Manifest adds `POST_NOTIFICATIONS` + `RECEIVE_BOOT_COMPLETED`. Channel created in `Application.kt`. Decision: notification shows **today's** remaining (`dailyBudget - spentFromDailyBudget`), not whole-period rest. Verified: `compileDebugKotlin`, `testDebugUnitTest` (4 new `DailyReminderContentTest` green), `assembleDebug` BUILD SUCCESSFUL; installed on API 36 emulator + granted `POST_NOTIFICATIONS`, app launches clean (actual-alarm E2E skipped per user). NOTE: receiver uses `goAsync()` + `Dispatchers.IO` — do NOT switch to `runBlocking` (AGENTS pitfall)
- **2026-08-06 History list animation crash FIXED (committed `e7770cd`, pushed)**: `updateAnimatedItemsState` (`ListAnimation.kt`) consumed DiffUtil's event stream to build the animated composite, but DiffUtil reports insert positions in the previous composite's coordinate space, which can still hold rows animating out — so `onInserted` read `newList[position + i]` out of bounds when typing in Search History or moving a record to another day. Reproduced on API 36 emulator: `Index 9 out of bounds for length 9` at `ListAnimation.kt:100`. Fix: composite built directly by key (reuse matching `AnimatedItem`, fresh instances for new rows, removed rows marked exit at their original spot); LazyColumn keyed by per-instance `AnimatedItem.id` (global `animatedItemIdCounter`) instead of `RowEntity.key`, eliminating the `Key "spent-6" was already used` variant too; dead `calculateDiff` wrapper + unused imports removed. Added `ListAnimationTest` (4 regression cases: moved-row instance reuse, search-drop-whole-days unique ids, in-place content edit, lingering rows no longer reading past `newList`). Verified: `testDebugUnitTest` green, `assembleDebug` BUILD SUCCESSFUL, on-emulator rapid search typing (b/u/s/backspace) no crash; user confirmed fixed on Pixel 7a / Android 17 (handed the fresh `app-debug.apk` after the earlier `657800f` build produced the `Key "spent-6"` crash). Working tree clean, branch up to date
- **2026-08-06 Editor date picker bounded to the spend period (committed `657800f`, pushed)**: user corrected the earlier past-entry range. The editor date picker (`DateTimeEditPill`) now allows only dates inside the budget period — `disableBeforeDate = startPeriodDate`, `disableAfterDate = finishPeriodDate` — and the calendar shows the period months (start month → end month). Example: period 1 Aug → 31 Aug, today 6 Aug → a 2 Aug record is addable, nothing outside the period. `CalendarState` default start reverted to current-month 1st (used only when no floor is set; the earlier one-year-back default is gone). Verified: `compileDebugKotlin`, `testDebugUnitTest`, `assembleDebug` all BUILD SUCCESSFUL
- **2026-08-06 Batch voice input (committed `38dcb4a`, pushed)**: a spoken list ("tea 20, lunch 150, dinner 300") parses into multiple records and opens a preview dialog with "Add all". AI prompt now asks for a JSON array; `parseVoiceAiContents` handles array/object/prose and falls back to the offline splitter. Offline `parseVoiceInputs` splits on commas/semicolons/newlines and on "and"/"then" only when both adjacent chunks carry a digit; thousands/decimal commas never split. `Keyboard` parks `List<VoicePending>` and commits all via `addSpent`; EDIT mode still applies only the first record as a replacement. `parseVoiceAiContent` (single) is kept as a delegate to the batch parser so the old tests stay valid. Verified: `testDebugUnitTest` (incl. 12 new cases) + `assembleDebug` BUILD SUCCESSFUL. Decision: user picked "Yes, also offline splitting" — offline batch is accepted as less reliable but AI-free
- **2026-08-06 Budget period start in the past (committed `5a48234`, pushed)**: the period date selector is now a two-tap range picker (`CalendarState.setSelectedDay` handles RANGE: first tap = start, second = end, restart on third; new `setSelectedRange`). `FinishDateSelector` signature is now `(selectStartDate, selectDate, onApply(startDate, finishDate))` and returns a `startDate` result; `BudgetConstructor` gained `startDateToValue` + `onChange(budget, startDate, finishDate)`; `Wallet` threads the start through Apply → `setBudget/changeBudget(budget, finish, start)` and includes it in `isChange`. `SpendsRepository.setBudget` dates the INCOME transaction at the chosen start (`roundToDay(newStartDate ?: today)`) and `changeBudget` updates `startPeriodDateStoreKey`. Guard: `Wallet` passes the start only if `it.time > 0L` so the fresh-budget `observeAsState(Date())` epoch sentinel doesn't create a 1970 period; `BudgetConstructor` also filters epoch dates out of the selector args. Verified: `compileDebugKotlin`, `testDebugUnitTest` (all green), `assembleDebug` BUILD SUCCESSFUL; `lintDebug` still pending. NOTE: the initial "past-day entries" edit (drop floor + one-year-back calendar) was later reverted per user — entries are bounded to the period instead
- **2026-08-06 Old tag-based categories card restored in Analytics**: the original comment/tag `CategoriesChartCard` renders below the AI `SpendCategoriesCard` (AI card unchanged and still working). User explicitly chose: re-add the old card as-is, no "Top items" title, no compare, no add button
- **2026-08-06 AI spend categories (committed `72db09f`, pushed)**: analytics-only predefined-category breakdown (FOOD/TRANSPORT/SHOPPING/BILLS/ENTERTAINMENT/HEALTH/TRAVEL/OTHER), AI-assigned via the Voice-AI endpoint, persisted to `transactions.category` (DB v11 migration), offline keyword fallback for instant display, new `SpendCategoriesCard` replaces the tag chart in Analytics, `SpendCategoriesViewModel` + 12 unit tests. Verified: `compileDebugKotlin` + `testDebugUnitTest` (12 new green) + `assembleDebug` BUILD SUCCESSFUL; `lintDebug` still pending
- **2026-08-06 Voice AI envelope unwrap (committed `2c90b8d`, pushed)**: chat-completions replies are parsed from `choices[0].message.content` (was reading top-level `content`), JSON keys read case-insensitively (`optStringIgnoreCase`), and any failure falls back to the offline parser — a prose/garbage AI answer no longer silently "succeeds" with an empty parse. Fix needed `testImplementation("org.json:json:20231013")` for JVM tests + one corrected test assertion; 53/53 tests green
- **2026-08-06 Analytics calendar full-month FIXED + history edit refresh (committed `f6e3649`, pushed)**: the period heatmap in `SpendsCalendar.kt` truncated at the current week. Root cause: the week-filter used `disabledAfter` (which is `.coerceAtMost(LocalDate.now())`, line 150) as the upper bound (`startOfWeek.isBefore(disabledAfter.plusDays(1))`), so any week whose start was today+1 or later was dropped — only the current/completed week of the month rendered. Fix: introduced `periodEnd = (actualFinishDate ?: finishDate).toLocalDate()` (uncapped) alongside `disabledAfter` (still capped to now, used only for gray-out), and the week filter is now `endOfWeek.isAfter(disabledBefore) && startOfWeek.isBefore(periodEnd.plusDays(1))`. Same commit: `History.kt`/`ListAnimation.kt` contentHash now includes value/comment/date (and day-total rows include the total), so edited transactions and day totals update in place instead of keeping stale rows. Verified: `assembleDebug testDebugUnitTest` BUILD SUCCESSFUL; on-emulator (API 36) via debug menu (keyboard "0"×8 + "." + apply toggles `isDebug`, then debug icon → "Open period summary screen") the Analytics calendar renders all 5 August week rows (2-8 … 30-31) including future weeks past today (Aug 6); Sep 1-5 cells are intentionally empty boxes (`isInMonth` only renders current-month days — existing design). Note for future UI automation: the wallet "View analytics" button is hard to reach because the editor sheet re-expands on swipes that cross its top edge (~y=1552); the debug-menu route works reliably
- **2026-08-06 carry-forward negative display FIXED (committed `3a83f7c`, pushed)**: the RecalcBudget "Congratulations! You saved" header showed a negative number even though the balance was positive. Root cause: `RecalcBudgetViewModel.calculateAddToToday` displayed `howMuchNotSpent() - nextDayBudget()`; when the ASK one-shot (`markDailyBudgetDistributionHandled`, commit `b9bdf01`) had already run for today, `lastChangeDailyBudgetDate = today` → `skippedDays == 0` and the subtraction = `(D - S) - (R - D)/(n-1)`, negative whenever the per-day rate exceeds today's leftover (real case: leftover ₹5.75, rate ≈ ₹156 → ≈ -₹150). Bug exists in upstream master too (introduced by `1a8328d`/`e524522`). Fix: new `SpendsRepository.howMuchSaved()` = `(R-D)/(restDays + skippedDays - 1) × max(skippedDays - 1, 0) + (D - S)` (last day: `R - S`), used by the ViewModel. 23/23 tests green; on-emulator day-change (clock shifted +1d, ASK fires, sheet opens) shows "Congratulations! You saved ₹5.75" — positive. Also shipped in the same commit: Voice AI error surfacing + `VoiceAiSettingsSheet` submenu, and recurring-payment currency display (`numberFormat` with active currency). Same commit carried the previously staged Voice AI + currency changes
- **2026-08-05 Android 17 launch crash FIXED (pushed `328dd94`)**: user's Pixel 7a / Android 17 opened the app and it instantly closed (no UI). Root cause found via a new `CrashLogger` (uncaught-exception handler writing to `Downloads/buckwheat-crash-*.txt`): `SpendsViewModel.changeDayMutex` was declared after the `init` block calling `runChangeDayAction()`, so the init-launched coroutine (Main.immediate, starts synchronously) called `changeDayMutex.withLock` on a null field → NPE at app startup (`SpendsViewModel.kt:409`). Moved the `Mutex` above `init`. Verified: 6/6 cold launches on API 36 emulator, `testDebugUnitTest` green. CrashLogger itself committed as `212f7cf`. NOTE: the 16 KB page-size theory was investigated and largely ruled out (both native libs are 16 KB-aligned); ps16k API 37 emulator couldn't boot on this host (disk space) and was deleted
- **2026-08-05 out-of-period import month buckets (committed `fc16848`, pushed)**: CSV rows outside the active budget period are archived into month buckets (`BudgetPeriod.isImported = true`, `budget = 0`) instead of the active table — grouped by calendar month, merged into covering archived periods, listed under Past Periods with an "Imported" label, searchable in spend search (non-blank query), and never counted against the current budget. DB version 10 (manual `ALTER TABLE` migration, Room 2.7.2 has no `@AddedColumn`). Verified: `testDebugUnitTest` 43/43 (20 SpendsRepositoryTest + 23 VoiceInputParserTest) + `assembleDebug` BUILD SUCCESSFUL
- **2026-08-05 bug-fix wave (since committed; details in `CHANGELOG.md`)**: 15 fixes shipped across voice AI, budget/period arithmetic, goals, tags, recurring payments, CSV import, and startup I/O — verified with `compileDebugKotlin` (BUILD SUCCESSFUL) + `SpendsRepositoryTest` (18/18)
- **Voice input hardening wave (since committed; details in `CHANGELOG.md`)**: AI call moved to `keyboard/VoiceAi.kt` (timeouts, JSON-safe body, markdown extraction, date fallback), `SpeechRecognizer` lifecycle hardened (`isRecognitionAvailable` + null-safe create/destroy), `voiceSession` stale-result guard in `Keyboard.kt`, voice messages i18n'd to `strings.xml` with permission/unavailable feedback, `VoiceInputParser` amount heuristic rewritten (currency-anchored, else last number; time stripped before amount), and 17 new `VoiceInputParserTest` cases — `testDebugUnitTest` fully green
- Last pushed commit `a309c29`: budget scope guards, voice AI off-main-thread, UI freeze fix, AGP 8.7.3 downgrade (Android Studio Narwhal 2024.2.1 supports max AGP 8.7.3)

## 2026-08-05 Bug-Fix Wave — Decisions
| Decision | Rationale |
|----------|-----------|
| Recurring backfill uses its own DataStore key `lastRecurringAppliedDate`, NOT `lastChangeDailyBudgetDate` | The ASK-branch no longer advances the budget date, so recurring needs an independent "already applied" marker |
| Recurring first run seeds the key **without** charging retroactively | A brand-new install must not suddenly insert a year of recurring payments |
| Backfill walks day-by-day (max 366 days guard) instead of only today's day-of-month | Payments are no longer skipped when the app is closed on the due day |
| Day-of-month 29/30/31 templates simply don't fire in shorter months (deliberate, not fixed) | Clamping to the last day would silently move the charge; day-of-month is the declared contract |
| ASK redistribution is a one-shot per day (`markDailyBudgetDistributionHandled()` writes `lastChangeDailyBudgetDate`) | Dismissing the sheet must not re-trigger every 5s poll and double-charge recurring |
| `removeSpent` picks the counter by `foldRanToday` (`lastChangeDailyBudgetDate` is today), not just `isSameDay(tx.date, today)` | The value's home (`spent` vs `spentFromDailyBudget`) depends on whether the daily fold ran, not the tx date |
| `archiveCurrentPeriod` filters to in-period rows | Out-of-period CSV imports / backfills must not be swept into the archived period |
| CSV import is idempotent via a (type, value, date, comment) signature set | Re-importing the same file must not duplicate rows; also dedupes within the file |
| `SavedTag.name` is now a unique index (migration 8→9 dedupes legacy rows first) | DB-level guarantee that the tag list can't have duplicates |
| Shared `parseAmountToBigDecimal()` lives in `util/numberExtensions.kt`; it distinguishes thousands ("1,234") from decimal comma ("12,50") | One parser used by voice, AI fallback, and CSV import |
| Voice commit in EDIT mode mirrors the Apply button (silent `removeSpent` + `addSpent`) | Voice must replace the edited transaction, not append a duplicate |
| Time parsing in `parseVoiceInput` requires a strong signal (at-prefix, colon-minutes, or am/pm suffix) | A bare number in a comment ("2 coffees") must not be read as a time |
| Finish-day boundary stays 23:59:59.999 (finish stored as `roundToDay(finish) + DAY - 1000`) | Deliberate: period is valid through the last millisecond of the finish day |
| AI date fallback to `Date()` when `tryParseVoiceAiDate` fails is acceptable | Malformed AI date → "now" is the sane default; no crash path |
| **`howMuchSaved()` uses `max(skippedDays - 1, 0)` on the saved-days multiplier** | `skippedDays == 0` (distribution already marked handled today) must yield `D - S`, not `(D - S) - (R-D)/(n-1)`; the old `howMuchNotSpent() - nextDayBudget()` was only valid for `skippedDays >= 1`. Formula matches the old result for `skippedDays >= 1` and last-day (`restDays == 0`) returns `R - S` |
| **Carry-forward display must not be "fixed" upstream-side only** | The subtraction bug lives in upstream master (`1a8328d` / `e524522`); we keep `howMuchSaved()` locally so we don't depend on an upstream merge |
| **Emulator day-change repro: shift the clock +1 day and let the ASK branch open the sheet** | Setting `cmd alarm set-time` to tomorrow makes `lastChangeDailyBudgetDate` "not today", so `runChangeDayAction` fires the ASK branch, `markDailyBudgetDistributionHandled` sets lastChange=today (→ skippedDays 0), and the sheet renders with exactly the buggy inputs. No DataStore binary editing or budget re-setup needed |
| Voice results-after-dispose is safe (rememberCoroutineScope cancels the launch on dispose) | May drop one in-flight result, never crashes — left as-is |
| AI parse lives in `keyboard/VoiceAi.kt` as `parseVoiceInputWithAiFallback` | One place owns timeouts (10s connect / 20s read), JSON-safe request building, markdown-fence extraction, and date fallback; every failure → `null` so the offline parser is the safety net |
| `Keyboard.kt` uses a monotonic `voiceSession` id + `isProcessing` flag to drop stale results | An AI reply arriving after a re-tap must not commit a second transaction or apply to the wrong session |
| `SpeechRecognizer` is created null-safely after `isRecognitionAvailable()` | `createSpeechRecognizer` can return null / throw on devices without a recognition service; UI shows "not available" instead of crashing |
| `parseVoiceInput` strips the time expression BEFORE picking the amount, and picks the currency-anchored number, else the LAST number | Fixes "2 coffees 150" → 150, and prevents "5pm"/"at 2pm" from being read as the amount |
| Time detection scans all candidates (`findAll` + strong-signal filter) instead of `find()` + `takeIf` | `find()` returns the first regex hit, which may be a weak quantity ("2 ") that wrongly suppresses a real time later in the string |
| Amount removal uses the digit group's range, not the full regex match | The regex's `\s*` between groups would swallow surrounding spaces ("tea 20 now" → "teanow") |
| `parseVoiceAiDate` must also accept no-offset `ISO_LOCAL_DATE_TIME` and `yyyy-MM-dd HH:mm:ss` | Models commonly return `2026-08-05T10:30:00` (no `Z`); before the fix those silently fell back to "now" |
| AI call shows "Understanding…" during the (up to 20s) request | The mic tap is blocked while processing, so without feedback the UI looks dead |
| Settings defaults for provider URL/model live in `SettingsRepository.kt` as shared constants (`DEFAULT_VOICE_AI_PROVIDER_URL`, `DEFAULT_VOICE_AI_MODEL`) + `normalizeVoiceAiModel(saved)`; every consumer (`SettingsRepository`, `VoiceAi.kt`, `VoiceAiSettingsSheet.kt`, `VoiceAiSettingsViewModel.kt`, `SpendCategorizer.kt`) reads them | **Centralized in `96feeec`** — this was previously a "two sources of truth" duplication (SettingsRepository vs VoiceAi.kt); the legacy `nvidia/nemotron-3-ultra-550b-a55b:free` default maps to the new model so saved settings auto-upgrade |
| Out-of-period CSV rows are archived into month buckets, never inserted into the active table | User requirement: old-month records grouped & listed under Past Periods, searchable in spend search, and excluded from the current budget |
| Month buckets use `budget = 0` + `isImported = true`; `totalSpent` stored at scale 2 | They represent archived history, not a live budget; 2dp matches the app-wide money convention |
| Archived rows appear in History/search only when the search query is non-blank (`composeHistoryRows`) | Keeps the default History view current-period-only while still making old records findable |
| Import idempotency signature (type, value, date, comment) covers both active and archived rows | Re-importing a file must not duplicate either the active table or the month buckets |
| DB v10 migration is manual `ALTER TABLE`, not `@AddedColumn` | Room 2.7.2 does not ship `@AddedColumn`; manual `Migration(9, 10)` in `MANUAL_MIGRATIONS` is required |
| `BudgetPeriod.id` is a class-body `var` (excluded from `copy()`) — never persist an object produced by `copy()` | The fake DAO's `updateTotalSpent` must re-apply the id after `copy(totalSpent = ...)` or the row orphans |
| **Class fields used by the `init` block must be declared ABOVE the `init` block** — Kotlin runs property initializers and `init` blocks in source order | `changeDayMutex` was declared after `init`, and the init-launched `Main.immediate` coroutine read it as null → NPE on launch (the Android 17 crash). Declaration order == initialization order |
| A debug-only crash logger (uncaught handler → `Downloads/buckwheat-crash-*.txt` + next-launch snackbar) is the fast path when a device crash can't be read via USB | No device logcat available; writing to public Downloads via MediaStore needs no permission and the user can share the file |
| The 16 KB page-size explanation was NOT the phone crash: both native libs are 16 KB-aligned, and the real cause was the `changeDayMutex` NPE race | Don't chase 16 KB next time this app misbehaves on a new Android version |
| **AI categorization persists only AI results, never offline keyword guesses** | `categorizeUncategorized` targets rows with null `category`; persisting keyword guesses would make them look "done" and the configured model would never refine them. Offline guesses are applied at display time only (`categoryFor`) |
| **Categories live in `transactions`, not `archived_transactions`** | Analytics for both active and just-finished periods reads the live table (archiving snapshots into `budget_periods` but Analytics is never rendered from archived rows), so one nullable column + v11 migration covers every case |
| **Replace the comment/tag `CategoriesChartCard`, don't keep both** | User requirement: predefined categories only, no manual entry — the free-text tag chart contradicts "no manual entry", so the AI-category card replaces it in Analytics (the old composable remains for its previews) |
| **Keyword matching is whole-word via `Pattern.quote` + `\b` boundaries** | A keyword like "eat" must never match "great"; the pattern map is built once and immutable for thread safety |
| **Batch classifier reuses the Voice AI OpenAI-compatible settings** | Same `voiceAiApiKey/ProviderUrl/Model` DataStore keys; no new settings UI. Empty API key → skip AI → offline keywords only |
| **`LaunchedEffect(spends)` keys the categorizer on the live period-spends list** | Re-fires when Room emits after an AI persist, but `categorizeUncategorized` no-ops on fully-categorized rows, so there is no loop |

## 2026-08-07 Feature Decisions
| Decision | Rationale |
|----------|-----------|
| Built-in categories get a fixed per-enum `emoji`; custom categories get a user-picked `emoji` column on `saved_categories` | No per-category table/emoji for built-ins is needed — they're enum constants; custom names are user-defined so the emoji must be stored with them (`emoji TEXT NOT NULL DEFAULT ''`, v12→v13 auto migration) |
| Compare-to-last-period is **day-aligned**: previous spend is cut at the same elapsed days as the current period | Comparing a mid-period total against the full previous period is misleading (early in a period it always looks like a huge improvement). `previousSpentAtSameElapsedDays` sums the previous period's SPENT archived rows with `date <= previousStart + (elapsedDays - 1)` |
| Compare card is hidden entirely when no previous (finished) period exists | `findPreviousPeriod` returns null → the card early-returns; an early `return` before `remember`s is safe because the call order stays stable across recompositions |
| Overspend notification fires **once per crossing** via `overspendNotifiedStoreKey` (`shouldNotifyOverspend(wasOver, nowOver, alreadyNotified)` = `nowOver && !wasOver && !alreadyNotified`) | Re-notifying on every spend while over the daily budget would spam; the flag resets when spending returns ≤ daily budget (today branch) and on `setDailyBudget` |
| Overspend flag is reset when a back-dated spend is added and when `setDailyBudget` runs | Deliberate: a fresh crossing (new day / re-fold) should be able to notify again. Known edge: adding a back-dated spend while still over today resets the flag and allows a re-notify |
| `OverspendNotificationSetting` mirrors `DailyBudgetReminderSetting` (switch + POST_NOTIFICATIONS permission flow) | Two opt-in toggles with the same UX; overspend is instant, the daily reminder is scheduled — separate channels (`overspending` vs `daily_budget_reminder`) and notification ids (200 vs 100) |
| Share summary is built by a pure `buildShareSummary(...)` (unit-tested) and triggered by `rememberShareSummary` on click | Keeps the message content testable without Android; formatting (`numberFormat`) is injected as a lambda |
| Analytics cards keep pure helpers as top-level functions in the card file | `weekdayAverageSpend`, `findPreviousPeriod`, `previousSpentAtSameElapsedDays`, `formatPercent`, `PeriodComparison`, `dailySpendTotals`, `monthDayCounts` are all JVM-unit-testable |
| Backup restore preserves entity ids and reinserts in FK-safe order | `archived_transactions.period_id` must keep linking to the reinserted `budget_periods` rows |
| Backup restore is NOT wrapped in a single DB transaction | Matches the existing multi-DAO `SpendsRepository` pattern; a mid-restore failure leaves partial state that a re-restore fixes |
| Rupee (INR) is the default currency on fresh installs; the world-currency picker shows a curated 12-code shortlist | User requirement (Indian app); the shortlist keeps the chooser usable and the current pick is prepended when outside the list |

## Architecture Decisions
| Single Activity | `MainActivity` with `setContent` |
| Sheet Navigation | Custom stack-based (AppViewModel.sheetStates) — no Jetpack Navigation |
| Custom Keyboard | Dedicated number pad instead of system keyboard |
| DataStore for Budget | Budget state stored in DataStore, not Room (avoids migration hell for frequent state) |
| Room for Transactions | Full transaction history in Room with schema migrations |
| Hilt DI | Singleton components for DB, repositories, ViewModels |

## Coding Rules (Enforced)
1. **NO `runBlocking`** — use `suspend` + coroutine scope (exception: `Keyboard.kt` uses `runBlocking` for commit, keep consistent with existing pattern)
2. **NO `!!` force-unwrap** — use `.getValue(key)` for maps, `as? T` for casts, `?:` with default for LiveData values
3. **NO `.first()` on potentially empty** — use `.firstOrNull()` with null check
4. **NO LiveData `.value` on background threads** — use `.asFlow().first()`
5. **NO `remember {}` without keys** — add observed state as key; prefer `observeAsState()` over `remember { mutableStateOf(value) }`
6. **NO manual Room DB creation** — use Hilt `@AndroidEntryPoint` + injected DAOs
7. **NO split DataStore `edit {}` calls** — combine into single block
8. **NO `!!` on LiveData `.value` in composables** — use `?:` safe default or null-guard before access

## Open Issues
- **None tracked** — `allowMainThreadQueries()` was removed (DAO methods are `suspend`; see `LESSONS.md` Issue 7).

## Implemented Features (on current master)
1. **Tag Management** — Persistent tags stored in Room `saved_tags` table (not lost on budget reset):
   - Settings → Tag Management opens CRUD bottom sheet
   - Tags merge transaction-derived tags with saved tags in `SpendsRepository.getAllTags()`
   - DB migration 5→6 adds `saved_tags` table
   - Merged tag list shown in `TagsManagementSheet` (transaction-derived tags have no delete; saved tags are editable)
2. **Date/Time editable on fresh entry** — DateTimeEditPill now renders in ADD mode; new spends use `editorViewModel.currentDate`
3. **Past dates in finish date selector** — `disableBeforeDate` set to `null` so users can create wallets with past finish dates
4. **Voice Input** — Mic button on keyboard uses Android `SpeechRecognizer` + `VoiceInputParser`:
   - Parses natural language utterances like "tea 20 now", "lunch 150 yesterday at 8pm"
   - Extracts amount, comment, and date/time
   - Auto-commits the transaction
5. **Analytics Calendar (month heatmap)** — Rewrote `SpendsCalendar.kt`:
   - Shows one month at a time with previous/next month navigation arrows
   - Each day cell is clickable (disabled dates outside budget period are grayed out)
   - Clicking a day closes analytics sheet and opens editor with that date pre-set
6. **Goal Tracking** — `SavingsGoal` entity + DAO + `GoalsSheet`/`GoalsViewModel`:
   - Create goals with name and target amount
   - Progress bar showing current/target
   - Allocate funds via dialog (creates SPENT transaction)
   - Auto-marks completed when target reached
7. **Recurring Payments** — `RecurringTemplate` entity + DAO + `RecurringPaymentsSheet`/`RecurringPaymentsViewModel`:
   - Create recurring expense templates with amount, comment, day-of-month
   - Enable/disable toggle per template
   - Auto-processed by `SpendsViewModel.processDueRecurringPayments()` — backfills every day since `lastRecurringAppliedDate` (so a closed app never skips a payment); first run seeds the key without retroactive charges
8. **Period-Scoped Analytics** — Analytics, History, and Wallet now use `periodSpends`/`periodTransactions` (filtered by current budget period start/end dates in DataStore)
9. **AI Spend Categories (analytics-only)** — Fixed predefined `SpendCategory` set (FOOD, TRANSPORT, SHOPPING, BILLS, ENTERTAINMENT, HEALTH, TRAVEL, OTHER), no user entry:
   - Assigned by the configured Voice-AI endpoint (`categorizeSpendsWithAi`, batches of 60) and persisted to `transactions.category` (DB version 11, manual `ALTER TABLE` migration); offline whole-word keyword classifier is the instant display fallback
   - `SpendCategoriesCard` in Analytics replaces the comment/tag-based `CategoriesChartCard`: donut + chips across all predefined categories, "Refining with AI…" progress bar while the batch runs
   - Runs on Analytics open via `SpendCategoriesViewModel.categorizeUncategorized(spends)` (LaunchedEffect keyed on spends); AI assignments are persisted, offline guesses are not, so a configured model keeps refining
10. **Custom categories + emojis** — user-managed categories stored in Room `saved_categories` (unique name, `emoji` column, DB v13):
    - Settings → Categories Management (CRUD, built-ins read-only with "Built-in" caption)
    - Editor category picker is an app-styled `CategorySelectorSheet` (Auto + built-ins + saved customs); `EmojiPicker` chips for add/edit
    - Custom categories show in the Analytics breakdown (`CategoryKey.BuiltIn/Custom` + `categoryTotals`), chips/rows render the emoji
11. **Full JSON backup & restore** — Settings rows "Back up data" / "Restore backup" (`backup/BackupData.kt` codec + `di/BackupRepository.kt`); covers all Room entities + both DataStore maps; restore wipes + reinserts FK-safe with confirmation dialog
12. **Share spending summary** — Analytics "Share summary" button opens the system share sheet (`analytics/ShareSummary.kt`, pure `buildShareSummary`)
13. **Compare to last period** — `CompareToLastPeriodCard` (this period vs previous at same elapsed days, ± amount + %) with a day-aligned pure helper
14. **Weekday spend breakdown** — `SpendsWeekdayCard` (avg per weekday over elapsed period, tap-to-highlight)
15. **Instant overspend notification** — `OverspendingNotifier` fires once when a spend crosses the daily budget (opt-in `OverspendNotificationSetting`, channel `overspending`, one-shot via `overspendNotifiedStoreKey`)

## Future Considerations
- Upstream has added features (recurring, categories, periods, notifications) that we may want to re-implement
- Our `our-fixes` branch contains working implementations that can be referenced

## Decisions (Session Compaction Fix)
| Decision | Rationale |
|----------|-----------|
| Plugin-based approach for compaction fix | Hooks into `experimental.session.compacting` to inject structured state without modifying core opencode |
| `.track/.session-state.json` for structured state | Lightweight JSON file that the plugin reads/writes to preserve nextMove, files, lastTask |
| `.track/` files as canonical source of truth | MEMORY.md (context), CHANGELOG.md (changes), CACHE.md (cache) are already maintained; plugin reads them rather than duplicating state |
| `.gitignore` the auto-managed state file | `.session-state.json` is transient and varies per session; not committed |
