# Changelog

## [Unreleased]

### Added
- **Categories management in Settings + editor category picker reworked** (user request: "categories management in setting like tag" + the M3 `DropdownMenu` didn't match the app UI): dynamic user-managed categories on top of the fixed built-in enum, plus the editor category picker re-styled as an app-consistent bottom sheet.
  - **Room**: new `SavedCategory` entity + `SavedCategoryDao` (`saved_categories`, unique `name` index) via manual migration 11→12 (`CREATE TABLE` + unique index, mirroring the 8→9 saved_tags migration); DB version bumped 11→12, added to `MANUAL_MIGRATIONS` and `AppModule`; `SpendsRepository` constructor gained `savedCategoryDao`
  - **Repository**: `getAllCategories()` merges distinct non-null `transactions.category` values (current period only — archived records carry no category column) with the saved custom names
  - **Management UI**: `CategoriesManagementViewModel` exposes `CategoryItem(name, id)` (`id == null` = built-in 8 `SpendCategory` entries or transaction-only residue); add/rename/delete reject blanks and names colliding with built-ins (`SpendCategory.fromStored` guard). `CategoriesManagementSheet` mirrors `TagsManagementSheet`: add field, built-ins shown read-only with a "Built-in" caption, saved ones get edit/delete, transaction-only ones get an add affordance to re-save them. Settings gained a Categories `TextRow` (`ic_label`) opening `CATEGORIES_MANAGEMENT_SHEET` (registered in `BottomSheets`)
  - **Editor picker**: `EditorViewModel.currentCategory` generalized from `MutableLiveData<SpendCategory?>` to `MutableLiveData<String?>` (prefill = `transaction.category`; 4 commit sites in `Keyboard.kt` changed `.value?.name` → `.value`). `CategorySelector` pill now opens a new app-styled `CategorySelectorSheet` — a bottom sheet with `CheckedRow` rows (Auto + built-ins + saved customs) matching `CurrencyEditor`, replacing the Material3 `DropdownMenu`; the currently-selected category is prepended when it no longer exists in the managed list (deleted custom category still shows as selected). New shared `categoryDisplayName(stored)` helper (built-in → localized `labelRes`, custom → raw stored name)
  - New strings: `categories_management_title/add_hint/edit/delete/builtin`, `category_selector_title`. **Known limitation:** custom categories aren't aggregated in the analytics `SpendCategoriesCard` (`categoryFor` only knows the 8 built-ins → custom spends land under keyword/OTHER there); documented, not fixed. Verified: `compileDebugKotlin`, `testDebugUnitTest` (94 incl. new `getAllCategoriesMergesTransactionCategories`), `assembleDebug` BUILD SUCCESSFUL
- **Currency: curated list + INR default (uncommitted)**: (1) the world-currency picker (`WorldCurrencyChooser.getCurrencies()`) no longer lists every available currency — it shows a curated shortlist of 12 important ones with INR first: INR, USD, EUR, GBP, JPY, CNY, AED, AUD, CAD, CHF, SAR, NZD. If the currently selected currency isn't in the shortlist it's still shown (prepended), so nobody loses their previous choice; the list is remembered keyed on `defaultCurrency`. (2) Rupee is now the default currency: `SpendsRepository.getCurrency()` falls back to `ExtendCurrency.getInstance("INR")` when no currency was ever stored, instead of `NONE`. This makes ₹ the default in onboarding/wallet setup, in the currency editor, and in formatting everywhere; explicitly choosing "No currency" still persists `""` → NONE, and archived periods store "INR" via `currentCurrencyCode()`. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **Analytics calendar day-tap fixed + heatmap scoped to the spend period (committed `756be1f`, pushed)**: two issues on the `SpendsCalendar` heatmap. (1) Tapping a spend day used to reset the editor, set the day as the new-spend date, and close analytics (`onClose()`) — which dropped the user back on home with the editor open. Now the tap opens the read-only `VIEWER_HISTORY_SHEET` (same pattern as `SpendsCountCard`) filtered to that day: `History` gained an `onlyDay: LocalDate?` filter (applied in `composeHistoryRows` before day-grouping), `ViewerHistory` accepts `onlyDay` via sheet args and shows the tapped date in the header (replacing "History"), `BottomSheets` passes `state.args["onlyDay"]`, and `Analytics.onDayClick` pushes `PathState(VIEWER_HISTORY_SHEET, args = mapOf("onlyDay" to date))`. Closing the viewer returns to analytics — no more navigation to home. Removed the now-unused `editorViewModel` param + `toDate` import from `Analytics`. (2) The heatmap month navigation was unbounded (`currentMonth` = now, arrows scrolled anywhere). It is now clamped to the spend period's months: `currentMonth` initializes to `YearMonth.now().coerceIn(firstPeriodMonth, lastPeriodMonth)` (start = `startDate` month, end = `actualFinishDate ?: finishDate` month) and the back/forward arrows are `enabled = false` at the boundaries. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **Period picker calendar limited to current + next month (uncommitted)**: user reported the onboarding/period calendar showed ~2 years of months. `FinishDateSelector` only set `disableBeforeDate`, so `CalendarState.listMonths` spanned from last month to `calendarEndDate` (Dec 31 of year + 2, ~27 months). Now it also sets `disableAfterDate = last day of next month` and floors the start at the 1st of the current month, so the picker renders exactly two months (current + next) and selection is bounded to that range. Verified: `compileDebugKotlin`, `testDebugUnitTest` (93), `assembleDebug` BUILD SUCCESSFUL
- **Trend chart polish on the monthly trend card (uncommitted)**: `SpendsTrendCard` gained three refinements — (1) **tap a bar** to highlight it (`colorMax.main`) and reveal a detail caption above the info row ("4 Aug · ₹150", date + day total); (2) a **dashed average line** across the bars at the average daily spend plus an `avg/day` caption in the middle of the bottom info row (new string `avg_per_day`, "avg/day %1$s"); (3) **month labels** under the bars when the period spans multiple calendar months (each label weighted by the day count in that month, e.g. "Jul · Aug"; shows "MMM ''yy" when the month isn't the current year). New pure helper `monthDayCounts(start, finish)` returns `List<Pair<YearMonth, Int>>` for the label weights (fixed bug where the first month was counted from its 1st instead of the period start). New `SpendsTrendBars` overload takes `averageDaily`, `selectedDay`, `highlightColor`, `averageLineColor`, `onDayTap` (tap detection via `detectTapGestures` + `pointerInput`). 4 new `SpendsTrendTest` cases (`monthDayCounts*`: single month, partial months, year boundary, reversed→empty). Verified: `compileDebugKotlin`, `testDebugUnitTest` (93 tests), `assembleDebug` BUILD SUCCESSFUL
- **Category picker in the editor reworked from a chip row into a compact dropdown** (user request: "editor looks complicated after the category row"): the always-visible horizontally-scrollable chip row is gone. `CategorySelector` is now a single collapsed pill ("Auto" + label icon + down-arrow) left-aligned in the editor; the dropdown only opens on tap and lists "Auto" + the 8 predefined categories (selected item highlighted in `primary`). Category assignment is fully opt-in — the collapsed pill shows the current selection (`Auto` when null, the category name when set) and the editor row height is back to ~36 dp. Commit wiring unchanged (`EditorViewModel.currentCategory` + `Keyboard.kt`). New string `category_auto`. Verified: `compileDebugKotlin`, `testDebugUnitTest`, `assembleDebug` BUILD SUCCESSFUL
- **Monthly trend card in Analytics** (user request: per-day bars / last-month comparison): new `analytics/SpendsTrendCard.kt` renders the current period's spend as a per-day bar chart (Canvas, rounded bars, one bar per day-of-period scaled to the day with the highest spend, gradient from `colorMax`→`colorMin`; zero-spend days get a tiny baseline nub) with start/end date captions. Header shows the period's total spent plus a "vs previous period" delta (+/−% colored via `colorBad`/`colorGood`) when a previous period exists. Comparison source: `previousPeriodBefore()` picks the most recent non-imported `BudgetPeriod` whose `finishDate` precedes the current period start (`budgetPeriods` exposed via new `SpendsRepository.getAllBudgetPeriods()` + `SpendsViewModel.budgetPeriods`; CSV-imported month buckets are excluded, and a zero-spend previous period hides the delta). Pure helpers `dailySpendTotals(spends, start, finish)` (out-of-period rows ignored via a `roundToDay(tx).before(start)` guard) and `previousPeriodBefore(periods, start)` are unit-tested. New strings `monthly_trend_title`, `vs_previous_period`. Verified: `compileDebugKotlin`, `testDebugUnitTest` (89 tests incl. 6 new `SpendsTrendTest` cases), `assembleDebug` BUILD SUCCESSFUL
- **Manual spend category in the editor** (user request): a horizontally-scrollable chip row of the 8 predefined categories now sits in the editor (between the tagging toolbar and keyboard). Picking a chip assigns that category to the record; tapping the selected chip again clears it back to auto (`null`). The choice is persisted on the transaction itself at commit time — ADD commits `category = currentCategory?.name`, EDIT commits it via `copy(...)` (prefilled from the stored category via `SpendCategory.fromStored`), and voice commits apply it too. `EditorViewModel` gained `currentCategory: MutableLiveData<SpendCategory?>` (reset on `startCreatingSpent`/`resetEditingSpent`, prefilled in `startEditingSpent`). New `editor/category/CategorySelector.kt` renders the chips (selected = `secondaryContainer` pill, else surface). Manual picks are never overwritten: the AI pass in `SpendCategoriesViewModel` only categorizes rows with `category.isNullOrBlank()`. No DB/migration change (`transactions.category` already exists, v11). Verified: `compileDebugKotlin`, `testDebugUnitTest` (all green), `assembleDebug` BUILD SUCCESSFUL
- **Daily budget reminder notification** (user request): optional daily notification with the budget left for today. New `notifications/` package:
  - `DailyBudgetReminderScheduler` — `AlarmManager.setRepeating` (`RTC_WAKEUP`, `INTERVAL_DAY`) at a user-chosen time (default 20:00); `cancel()` for the toggle-off path; the alarm uses the PendingIntent `FLAG_IMMUTABLE` and requests `REQUEST_CODE`/`NOTIFICATION_ID` 100
  - `DailyBudgetReminderReceiver` — fires on the alarm, reads `budgetDataStore` (`dailyBudget`, `spentFromDailyBudget`, `currency`) inside `goAsync()` + `CoroutineScope(Dispatchers.IO)` (no `runBlocking`), builds the message with `buildDailyReminderMessage`, formats with `numberFormat`, and posts a `NotificationCompat` (channel `daily_budget_reminder`, icon `ic_stat_notification`) that taps into `MainActivity`
  - `DailyReminderContent` — pure builder: `dailyBudget - spentFromDailyBudget` → LEFT ("You have ₹350 left today"), negative → OVER ("Daily budget spent — ₹100 over", absolute amount), no budget set → NO_BUDGET hint
  - `ReminderBootReceiver` — `BOOT_COMPLETED` re-schedules the alarm when enabled (reads `reminderEnabled/hour/minute` from `settingsDataStore`)
  - `DailyBudgetReminderSetting` — Settings toggle row (Switch) + a time row (opens the existing `TimePickerDialog`); enabling on API 33+ requests `POST_NOTIFICATIONS` via `rememberLauncherForActivityResult` and only activates if granted; persisting via `SettingsRepository.switchReminderEnabled`/`setReminderTime` and new keys `reminderEnabled`/`reminderHour`/`reminderMinute`
  - `Application.kt` creates the notification channel; manifest adds `POST_NOTIFICATIONS` + `RECEIVE_BOOT_COMPLETED` and the two receivers
  - 4 new `DailyReminderContentTest` unit tests (left/zero/over/no-budget). Verified: `compileDebugKotlin`, `testDebugUnitTest` (4/4 new green), `assembleDebug` BUILD SUCCESSFUL; installed + granted `POST_NOTIFICATIONS` on the API 36 emulator, app launches clean (emulator E2E of the actual alarm skipped per user)
- **Batch voice input — add 3-4 records at once** (user request): a spoken list like "tea 20, lunch 150, dinner 300" now parses into several records and opens a preview dialog with "Add all". AI path: the system prompt asks for a JSON array `[{amount, comment, date}, …]` and `parseVoiceAiContents` handles arrays (also single objects and prose, falling back to the offline splitter). Offline path: `parseVoiceInputs` splits on commas/semicolons/newlines and on "and"/"then" only when both adjacent chunks carry a digit (so "bread and butter 50" stays one record while "tea 20 and lunch 150" becomes two); thousands/decimal commas ("1,234", "12,50") are never treated as separators. Keyboard flow parks `List<VoicePending>` and commits every record via `addSpent` (EDIT mode still replaces just the edited transaction with the first record). 12 new `VoiceInputParserTest` cases. Verified: `testDebugUnitTest` + `assembleDebug` BUILD SUCCESSFUL
- **Budget period can start in the past** — the period date selector in the wallet (date button) is now a two-tap range picker: tap the start day, then the end day. The start date may be before today (calendar opens near the current month, floor is one month back), so a new/edited period can begin e.g. 1 Aug even when today is 6 Aug. The period start is persisted: the opening INCOME transaction is dated at the chosen start date and `changeBudget`/`setBudget` update the stored `startPeriodDate` (fresh periods without a chosen start still default to today). `CalendarState` gained two-tap `RANGE` selection + `setSelectedRange(start, end)`; `FinishDateSelector` now returns `(startDate, finishDate)` and `BudgetConstructor`/`Wallet` thread the start date through to the repository
- **Spend entries can be added for any day inside the spend period** — the spend editor's date picker now restricts selection to the budget period: `disableBeforeDate = period start` and `disableAfterDate = period end`, and the calendar shows exactly the period's months (start month → end month). So with a period starting 1 Aug and today 6 Aug, a 2 Aug record can be added, but nothing outside the period. (Later corrected per user: not "month-1st-to-today", and not the earlier no-floor/one-year-back behavior.) `CalendarState` default start was reverted to the current month (`LocalDate.now().withDayOfMonth(1)`), used only when no floor is set
- **AI spend categories card kept alongside the tag-based categories card** — the AI `SpendCategoriesCard` stays as-is, and the original comment/tag `CategoriesChartCard` (donut + tag chips) is shown again below it in Analytics, so both breakdowns render on the same screen
- **AI spend categories in analytics** (user request: predefined categories only, AI-assigned, no manual entry):
  - New fixed `SpendCategory` enum (FOOD, TRANSPORT, SHOPPING, BILLS, ENTERTAINMENT, HEALTH, TRAVEL, OTHER) with per-category keyword lists and localized labels (`category_*` strings)
  - New `SpendCategorizer.kt` — `offlineClassify(comment)` deterministic whole-word keyword fallback, `categoryFor(tx)` = persisted AI category else offline guess, and `categorizeSpendsWithAi(context, spends)` batch classifier that reuses the Voice AI OpenAI-compatible endpoint (same API key / provider URL / model settings), chunked 60 records per call, and falls back silently to keywords on any failure
  - `transactions.category` column (DB version 11, manual migration `ALTER TABLE transactions ADD COLUMN category TEXT`) + `TransactionDao.updateCategory(uid, category)`
  - New `SpendCategoriesViewModel` persists AI assignments only (never offline guesses) so a configured model keeps refining uncategorized rows on later opens
  - New `SpendCategoriesCard` in Analytics replaces the comment/tag `CategoriesChartCard`: donut + chip breakdown across all predefined categories with a "Refining with AI…" progress bar while the batch runs
  - `extractJsonContent` promoted to `internal` in `VoiceAi.kt` for reuse
  - 12 new `SpendCategorizerTest` cases (keyword mapping, substring safety, persisted-category precedence, `fromStored` parsing, response envelope parsing)
- **Crash-log capture** — new `CrashLogger.kt`: an `UncaughtExceptionHandler` installed in `Application.onCreate` writes any uncaught crash stack trace to `Downloads/buckwheat-crash-*.txt` (via MediaStore, no permissions needed) and persists it, so the built-in crash-report snackbar can show a "send crash report" prompt on the next successful launch. Used to diagnose a launch crash on Android 17 / Pixel 7a without USB access
- **Out-of-period CSV imports are archived into month buckets** (user request: old records grouped by month, listed under Past Periods, searchable, never counted in the budget):
  - `BudgetPeriod.isImported` column (DB version 10, manual migration `ALTER TABLE budget_periods ADD COLUMN is_imported INTEGER NOT NULL DEFAULT 0`; Room 2.7.2 has no `@AddedColumn`)
  - `SpendsRepository.importTransactions` routes in-period rows to `addSpent`, out-of-period rows to new `archiveImported()` — grouped by calendar month into `BudgetPeriod(budget = 0, isImported = true)` buckets or merged into a covering archived period; `totalSpent` stores SPENT sum at scale 2
  - Import idempotency now also covers already-archived rows, so re-importing a file never duplicates month-bucket entries
  - `BudgetPeriodDao`: `getAllNow()`, `getAllArchivedNow()`, `getAllArchived()`, `updateTotalSpent()`; shared `ArchivedTransaction.toTransaction()` extension
  - `SpendsViewModel.archivedTransactions` exposed; History search merges archived rows when the query is non-blank (`composeHistoryRows`)
  - `PastPeriodsSheet` shows "Imported" label; `PeriodDetailSheet` hides budget cards for imported buckets (shows date range); new string `past_periods_imported`
- Date/time pill now visible and editable on fresh entry (ADD mode) — `Editor.kt:50`, `DateTimeEditPill.kt:29`
- New spends in ADD mode use `editorViewModel.currentDate` instead of `Date()` — `Keyboard.kt:317`
- **Voice input feature** — microphone button on the keyboard:
  - New `VoiceInputParser.kt` — parses natural language (e.g. "tea 20 now", "lunch 150 yesterday at 2pm") into amount, comment, and date
  - New `ic_mic.xml` microphone vector icon
  - `RECORD_AUDIO` permission in `AndroidManifest.xml`
  - Mic bar at top of keyboard with speech recognition via `SpeechRecognizer`
  - Auto-parses transcription, fills editor fields, and commits the transaction immediately
- Finish date selector now allows past dates (`disableBeforeDate = null`) — `FinishDateSelector.kt:47`
- **Analytics calendar** — heatmap now shows one month at a time with `<` `>` navigation; each day is clickable. Clicking a day closes the analytics sheet and opens the editor with that date pre-set (uses `EditorViewModel` directly via shared Hilt scope)
- **Goal tracking** — `SavingsGoal` entity, DAO, bottom sheet (`GoalsSheet`), ViewModel (`GoalsViewModel`):
  - CRUD for savings goals with target amount and current progress
  - Linear progress indicator per goal
  - Manual allocation dialog (creates a SPENT transaction with the goal name)
- **Recurring payments** — `RecurringTemplate` entity, DAO, bottom sheet (`RecurringPaymentsSheet`), ViewModel (`RecurringPaymentsViewModel`):
  - CRUD for recurring expense templates with amount, comment, day-of-month
  - Enable/disable toggle per template
  - Auto-processed on day change in `SpendsViewModel.runChangeDayAction()`
- **Period-scoped analytics** — `Analytics.kt` and `History.kt` now use `periodSpends`/`periodTransactions` (filtered to current budget period) instead of all transactions
- **Date range queries** — `TransactionDao` now exposes `getAll(type, startDate, endDate)` and `getAll(startDate, endDate)` for period scoping
- **Tag management merge** — `TagsManagementSheet` now shows transaction-derived tags alongside saved tags; `TagItem` data class merges both sources

### Changed
- Fresh fork from upstream/master @ `4b60102` — clean slate
- All prior work saved to `our-fixes` branch
- Restored CI build workflow (`.github/workflows/build.yml`)
- Added `.track/` directory with AGENTS.md, CHANGELOG.md, ARCHITECTURE.md, MEMORY.md, CACHE.md, CODE_FLOW.md
- Added session compaction fix plugin (`.opencode/plugins/compaction-fix.ts`) — preserves task state, decisions, and context across opencode session compactions
- Added `.track/.session-state.json` — auto-managed state snapshot for compaction recovery
- Updated `.track/AGENTS.md` section 8 with compaction recovery protocol
- Updated `.opencode/skills/buckwheat/SKILL.md` with compaction recovery section
- Added `.track/.session-state.json` to `.gitignore`
- Added tag management feature (persistent tags via Room `saved_tags` table):
  - New `SavedTag` entity + `SavedTagDao` for persistent tag storage
  - Database migration 5→6 creates `saved_tags` table
  - New `TagsManagementSheet` bottom sheet with scrollable CRUD list (add, edit, delete)
  - "Tag Management" option added to Settings page (opens via gear icon → Settings)
  - Saved tags merged with transaction-derived tags in `SpendsRepository.getAllTags()`
  - Tags survive budget resets (no longer lost when `transactionDao.deleteAll()` is called)
  - New string resources for tag management UI
- DB version bumped to 8 — auto-migration 7→8 adds `recurring_templates` and `savings_goals` tables
- `SpendsViewModel` now injects `RecurringDao` for processing recurring payments on day change
- `SpendsViewModel` added `periodSpends` and `periodTransactions` (MediatorLiveData filtering by budget period)
- Added `filterByPeriod()` helper in `SpendsViewModel`

### Fixed
- **History list crash when searching or editing a record's date** — `updateAnimatedItemsState` (`ListAnimation.kt`) consumed DiffUtil's event stream to build the animated composite list, but DiffUtil reports insert positions in the coordinate space of the previous composite, which can still contain rows animating out (a keystroke cancels the previous exit animation). Those lingering rows inflated the positions beyond `newList.size`, so `onInserted` read `newList[position + i]` out of bounds → `IndexOutOfBoundsException` when typing in Search History or moving a record to another day. Reproduced on emulator (API 36): `Index 9 out of bounds for length 9` at `ListAnimation.kt:100`. Fix: the composite is now built directly by key instead of via DiffUtil dispatch — kept rows are reused in place (with content update), new rows get fresh instances, removed rows are marked `targetState = false` and inserted near their original spot for the exit animation. LazyColumn items are also keyed by a per-instance `AnimatedItem.id` (unique counter) instead of `RowEntity.key`, so a moved row never yields a duplicate key (`Key "spent-6" was already used` — the second reported variant, confirmed fixed on the user's Pixel 7a / Android 17 after they installed the fresh build). Dead `calculateDiff` wrapper removed. Added `ListAnimationTest` (4 regression cases: moved row, search-filter dropping whole days, in-place content edit, lingering rows from a previous frame). Verified: `testDebugUnitTest` + `assembleDebug` BUILD SUCCESSFUL; on-emulator rapid search typing (b/u/s/backspace through filter transitions) no longer crashes
- **Analytics calendar truncated at the current week** — `SpendsCalendar.kt` capped the week-filter end at `LocalDate.now()`, so any week whose start was today+1 or later was dropped and only the current/completed week of the month rendered. The week filter now uses the real period end (`periodEnd = (actualFinishDate ?: finishDate)`) instead of the now-capped `disabledAfter`, so the full month grid renders including future weeks of the current period. Days after today remain grayed-out via the still-capped `disabledAfter`. Verified on emulator (API 36): Analytics shows all 5 August week rows (2-8, 9-15, 16-22, 23-29, 30-31); previously only 2-8 appeared
- **Launch crash on Android 17 (open-then-close)** — `SpendsViewModel.changeDayMutex` was declared *after* the `init` block that calls `runChangeDayAction()`. The init-launched coroutine (on `Dispatchers.Main.immediate`) could start synchronously and call `changeDayMutex.withLock { ... }` before the field was initialized → `NullPointerException` in `runChangeDayAction` (`SpendsViewModel.kt:409`) during the ViewModel constructor, killing the app at startup before any UI rendered. `changeDayMutex` is now initialized above `init`
- **Month-bucket `totalSpent` scale mismatch** — `archiveImported` stored scale-0 sums (10) while the app/tests expect 2dp (10.00); now stores `(currentTotal + spentDelta).setScale(2)`
- **`FakeBudgetPeriodDao.updateTotalSpent` dropped the period id** — `BudgetPeriod.id` is a class-body `var` (not a constructor property), so `copy(totalSpent = ...)` resets it to 0, orphaning the archived rows; the fake now re-applies the id after copy
- **Main-thread blocking on startup reads** — `syncTheme` (`Theme.kt`) and `syncOverrideLocale` (`Locale.kt`) called `runBlocking { dataStore.data.first() }` on the main thread from `LaunchedEffect`; both are now `suspend` and read directly. `SpendsViewModel.init` also replaced its `runBlocking` pre-read with a `viewModelScope.launch`
- **`restBudget` flashed intermediate values on startup** — `SpendsViewModel.restBudget` recomputed on every source emission, briefly showing a wrong "rest" while the budget/spent flows streamed in; it now emits nothing until all three sources have produced their first value
- **Empty (no-spend) periods were never archived** — `SpendsRepository.archiveCurrentPeriod` bailed on `spends.isEmpty()`, dropping income-only periods from the archive history; it now archives whenever the period has any in-period rows (`inPeriod.isEmpty()` guard)
- **Re-importing the same CSV created duplicate transactions** — `SpendsRepository.importTransactions` now skips rows whose (type, value, date, comment) already exist and dedupes repeated rows within the file, so import is idempotent
- **Voice parser mangled thousands separators and misread comment numbers as times** — `VoiceInputParser`: the amount regex replaced "," with "." (so "1,234 rupees" parsed as 1.234) and any bare number in the comment ("2 coffees") was treated as a time of day, corrupting the transaction date. The raw amount now flows through `parseAmountToBigDecimal` (which distinguishes thousands vs decimal comma), and a time is only recognized with a strong signal: an "at" prefix, a colon-separated minute, or an explicit am/pm suffix
- **Mic button could double-start the speech recognizer** — `Keyboard.kt`: tapping the mic while already listening called `startListening()` again, which can crash the recognition session (`IllegalStateException`). A tap while `isListening` is now ignored
- **Widget and wallet crashed on non-terminating division** — `CommonWidgetReceiver.whatBudgetForDay` (`splitBudget / restDays`) and `Total` (`budget / days`) called `BigDecimal.divide` without a scale/rounding mode, throwing `ArithmeticException: Non-terminating decimal expansion` whenever the division did not terminate exactly. Both now use `.divide(..., scale, RoundingMode)` in a single call
- **Day-change action could run twice** — `SpendsViewModel.runChangeDayAction` is triggered from `init` and from the 5s polling loop; since it suspends on DataStore reads, two overlapping runs could both pass the "day changed" check and redistribute the budget / charge recurring payments twice. The whole action now runs under a `Mutex`
- **`setDailyBudget` crashed on missing counters** — `SpendsRepository.setDailyBudget` used `!!` on `spent` / `spentFromDailyBudget`, throwing an NPE if the keys were absent; now falls back to `BigDecimal.ZERO`
- **Duplicate tags could be created** — `SavedTag` now has a unique index on `name` (DB migration 8→9, deduping legacy rows first), `SavedTagDao` gained `getByName`/`existsByName`, and `TagsManagementViewModel` skips `addTag` when the name already exists and refuses to rename a tag onto an existing tag's name
- **Goal allocation could double-spend the budget** — `GoalsViewModel.allocateToGoal`: two allocations racing could both read the same `budgetRest`, both pass the `budgetRest < amount` guard, and together exceed the remaining budget. Allocations are now serialized with a `Mutex`; the check-then-act (rest check → goal update → `addSpent`) runs inside a single lock
- **Voice input while editing discarded the edit** — `Keyboard.kt onResults`: in EDIT mode, voice parsing only filled the amount field and called `addSpent`, so the commit appended a second spend while leaving the edited transaction untouched (and the amount was never committed to the edited record). The voice commit now mirrors the Apply button: in `EditMode.EDIT` it replaces the edited transaction (silent `removeSpent` + `addSpent` of the parsed copy); otherwise it appends as before
- **Period archive wiped out-of-period transactions** — `SpendsRepository.archiveCurrentPeriod`: it archived every row in the table, so CSV imports / backfilled spends outside the current period were swept into the archived period and vanished from the active History. It now filters to transactions within `[startPeriodDate, finishPeriodDate]` before archiving (and totals spent only from those); out-of-period rows stay in the active table
- **Removing a previous-day spend drained the wrong counter** — `SpendsRepository.removeSpent`: the branch was chosen by the transaction's date alone (`isSameDay`), so deleting a spend dated before today while today's daily-budget fold had not yet run subtracted from `spent` even though the value still lived in `spentFromDailyBudget`, driving `spent` negative and leaking the stale value. The counter is now selected by whether the fold already ran today (`lastChangeDailyBudgetDate`), with a floor of zero on both counters
- **ASK mode stuck → recurring double-charged every 5s** — `SpendsViewModel.kt:258-301`: if the user dismissed the "recalculate budget" sheet, `lastChangeDailyBudgetDate` never advanced, so the day-change branch re-ran every poll and re-inserted recurring transactions. Now the ASK branch records the day as handled (`SpendsRepository.markDailyBudgetDistributionHandled()`) and is a one-shot per day
- **Recurring payments skipped when app closed on due day** — `SpendsViewModel.processDueRecurringPayments()`: recurring is now tracked by its own `lastRecurringAppliedDate` DataStore key and backfills every day since the last application (up to 366 days), charging each due day's templates with the correct date, instead of only checking the current day-of-month
- **Crash from `setScale(2)` on 3+ decimal values** — `SpendsRepository.kt:96-108` and `numberExtensions.kt isEquals` used `setScale(2)` with default `UNNECESSARY`, throwing `ArithmeticException` whenever a stored value (e.g. from CSV `10.005`) had more than 2 decimals, killing the budget flows. Now `setScale(2, RoundingMode.HALF_EVEN)`; CSV import (`rememberImportCSV.kt:72`) also normalizes amounts via shared `parseAmountToBigDecimal()` (moved to `util/numberExtensions.kt`) and rounds to 2dp
- **Goals sheet crash on progress divide** — `GoalsSheet.kt:187-192`: `currentAmount / targetAmount` used `BigDecimal.divide()` without scale → `ArithmeticException` on non-terminating quotients (e.g. 100/300). Now `.divide(target, 2, RoundingMode.HALF_EVEN)`
- **Voice AI crash on malformed amount** — `Keyboard.kt:210-245`: AI output like `"150.5 USD"` / `"₹150"` / `"12,50"` crashed with `NumberFormatException` (`parsed.amount.toBigDecimal()`). Now sanitized via `parseAmountToBigDecimal()` (`VoiceInputParser.kt`), zero/blank guarded numerically, and the whole commit block is wrapped in try/catch
- **NPE crash on budget setup** — `Wallet.kt:53` `budget.value!!` and `Wallet.kt:308` `currency!!` in apply button handler could NPE if LiveData hadn't emitted yet; replaced with safe-access patterns
- **NPE crash on CSV export** — `rememberExportCSV.kt` used `remember { mutableStateOf(...) }` without keys, capturing null dates permanently; `DateTimeFormatter.format(null)` crashed. Changed to `observeAsState()` with null-safe formatting
- **NPE in CurrencyEditor** — `CurrencyEditor.kt:43` `spendsViewModel.currency.value!!` could NPE; replaced with safe default
- **Force-unwrap `spends!!`** in Wallet display — replaced `spends!!.isNotEmpty()` with `spends?.isNotEmpty() == true`
- **Force-unwrap `currency!!`** in Wallet currency caption — rewrote with `when (val c = currency)` null-safe pattern
- **Force-unwrap `showAllocateDialog!!`** in `GoalsSheet.kt:157` — replaced with local val capture pattern
- **Hardcoded UI strings** in `Settings.kt`, `GoalsSheet.kt`, `RecurringPaymentsSheet.kt` — moved all text to `strings.xml` with `stringResource()` references (10 new string resources added)
- **Missing stringResource imports** — added `stringResource` imports to `GoalsSheet.kt` and `RecurringPaymentsSheet.kt`
- **Voice AI call could hang forever** — the AI parse ran on `HttpURLConnection` with no connect/read timeout, so a dead provider URL could stall the main-dispatched coroutine indefinitely. New `keyboard/VoiceAi.kt` (`parseVoiceInputWithAiFallback`) adds 10s connect / 20s read timeouts, guarantees `disconnect()` in `finally`, builds the request via `JSONObject` (so quotes/newlines in the transcript are escaped instead of interpolated into a raw string), extracts JSON even when wrapped in markdown fences or prose, validates the response shape, and falls back to `now` for unparseable dates — every failure returns `null` so the offline parser takes over
- **`SpeechRecognizer` could NPE / crash on devices without a recognition service** — `Keyboard.kt` created the recognizer unconditionally. It now checks `SpeechRecognizer.isRecognitionAvailable()` first, creates it null-safely, and null-guards `startListening`/`destroy`
- **Stale voice result could commit twice / out of order** — an AI reply that arrived after the user re-tapped the mic (or tapped again while a result was being committed) would apply to a transaction from the previous session. `Keyboard.kt` now tracks a monotonic `voiceSession` id (bumped on every `startListening`), discards results whose session no longer matches, and blocks re-taps while a result is still processing (`isProcessing`)
- **Voice status messages were hardcoded English strings** — `"Listening..."`, `"No speech heard"`, `"Couldn't understand"`, `"Recognition failed"`, `"Voice input"` moved to `strings.xml` (`voice_*`); mic-tap now also reports microphone-permission denial and "recognition not available on this device"
- **Voice parser picked the FIRST number as the amount** — `"2 coffees 150"` parsed as 2. `VoiceInputParser` now strips the time-of-day expression before amount extraction (so `"5pm coffee 5"` yields 5, not a 17:00 time read as an amount), prefers the number tied to a currency marker (`₹`, `$`, `€`, `£`, `¥`, `rupees`, `dollars`, …), and otherwise takes the LAST number; time detection also scans all candidates instead of rejecting on the first weak match
- **Added `VoiceInputParserTest`** — 17 unit tests covering amount/comment/date extraction, thousands vs decimal separators, currency anchoring, am/pm and `at`-prefixed times, yesterday/tomorrow, and null inputs
- **AI voice-input review pass**:
  - `parseVoiceAiDate` now parses the common no-offset `ISO_LOCAL_DATE_TIME` (`2026-08-05T10:30:00` / `T10:30`) and `yyyy-MM-dd HH:mm:ss`; previously those fell back to "now", silently discarding the date the model returned. Also dropped the duplicate `yyyy-MM-dd` formatter (`ISO_LOCAL_DATE` is the same)
  - "Understanding…" (`voice_processing`) is shown while the AI/parse result is being applied, so the user isn't left staring at a silent keyboard for the up-to-20s AI call (taps are blocked during processing); it renders in the neutral color, not error red
  - Added 6 `parseVoiceAiDate` unit tests (ISO offset/local, space-separated with seconds, plain date, relative words anchored to `now`, blank/garbage fallback)
- **Test count**: `VoiceInputParserTest` now 23 cases (17 parser + 6 AI date), all green alongside `SpendsRepositoryTest` (18) and `assembleDebug`
- **Carry-forward ("You saved") showed a negative amount despite a positive balance** — the RecalcBudget sheet's header (`RecalcBudgetViewModel.calculateAddToToday`) computed `howMuchNotSpent() - nextDayBudget()`. When the ASK one-shot had already marked the daily-budget distribution handled for today (`markDailyBudgetDistributionHandled()` sets `lastChangeDailyBudgetDate = today` → `skippedDays == 0`), that subtraction became `(daily - spentToday) - (restBudget - daily)/(restDays-1)`, spuriously negative whenever the per-day rate exceeded today's leftover (e.g. leftover ₹5.75, rate ~₹156 → showed ≈ -₹150). New `SpendsRepository.howMuchSaved()` returns `(R-D)/(restDays + skippedDays - 1) × max(skippedDays - 1, 0) + (D - S)` (or `R - S` on the last day), which equals the old formula for `skippedDays >= 1` but correctly `D - S` for `skippedDays == 0`. `RecalcBudgetViewModel` now displays `howMuchSaved()`. Regression tests `carryForwardStaysPositiveAfterDistributionHandledToday` and `carryForwardStaysPositiveWithLargeBudget` (the real 5000/156.25/150.50/5.75 scenario) added. Verified: 23/23 tests, `assembleDebug`, and on-emulator day-change (clock shifted +1 day → sheet opened showing "Congratulations! You saved ₹5.75" instead of negative)
- **Voice AI failures silently succeeded** — a non-2xx AI response or a failure inside the AI parse produced an empty result that was treated as a successful "no voice" outcome. `VoiceAi.kt` now returns a sealed `VoiceAiResult` (`Success` / `Failure` with truncated error body / `NotConfigured`), `Keyboard.kt` surfaces `voice_ai_error_prefix` in red when the AI call fails, and a new `VoiceAiSettingsSheet` (`VOICE_AI_SETTINGS_SHEET`) submenu on the Settings page holds API key / URL / model (moved out of `Settings.kt`). New strings `voice_ai_title`, `voice_ai_error_prefix`
- **Recurring payments always rendered "$"** — `RecurringPaymentsSheet.kt` formatted template amounts with a hardcoded dollar sign; it now injects `SpendsViewModel`, observes `currency`, and renders via `numberFormat(context, template.amount, currency)` (verified ₹100 row end-to-end)
- **History kept stale rows after editing a transaction** — `History.kt`/`ListAnimation.kt` used a constant `contentHash` per row (`"spent-${tx.uid}"`), so an in-place edit (value/comment/date change) never invalidated the animated row and the old values lingered. Row `contentHash` now includes value/comment/date (day-total rows include the total), so edited rows and totals refresh in place
- **Chat-completions replies were parsed from the wrong JSON field** — OpenAI-style responses wrap the text in `choices[0].message.content`, but `VoiceAi.extractModelContent` read the top-level `content`, so voice AI effectively always fell back to the offline parser. It now unwraps `choices[0].message.content`, reads keys case-insensitively (`optStringIgnoreCase`), and treats prose/garbage replies as a failure → offline fallback (never an empty "success"). Required `testImplementation("org.json:json:20231013")` (JVM stubs Android's `org.json`) and one corrected test assertion

### Known State
- Room DB is at **version 11** (entities: transactions, storage, saved_tags, budget_periods, archived_transactions, recurring_templates, savings_goals); schema exports tracked under `app/schemas/.../DatabaseModule/{1..11}.json`
- No sync, no weekly/monthly analytics notifications (upstream-only features; see Future Considerations in `MEMORY.md`)

## Removed (from our-fixes to fresh fork)
- Removed all notification system code (NotificationScheduler, notification channels, AlarmManager)
- Removed recurring transactions (RecurringDao, RecurringRepository, RecurringReceiver)
- Removed period tracking (PeriodDao, Period entity)
- Removed category/tag system (CategoryDao, Category entity, TagCategory)
- Removed sync/export (SyncManager, SyncReceiver)
- Removed reminder system (ReminderReceiver)
- Removed all prior fix commits (saved to our-fixes branch)
